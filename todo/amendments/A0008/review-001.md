# A0008 owner amendment review

- Base commit: `02c8f16ef1b0507f7006914090d608650908f179`
- Candidate commit: `a0e4ea92fe4f48949d5c4b6a89562ca0fb0a7e9f`
- Verdict: **FAIL**

## Summary

Independent PROPHET-change review of A0008 (layer PROPHET, task_ids [], target_status null), a documentation-integrity correction that also creates T005 and T006. Identity: the candidate is a single commit whose only parent is the base 02c8f16ef1b0507f7006914090d608650908f179, the review worktree is clean at exactly that SHA, and /home/lpdev/lp HEAD is 02c8f16 as the change's audit states. Six of the seven checks pass; coherence fails on two points, both of them fixable inside the change's own edited surface.

SCOPE (pass). git diff --name-status base..candidate reports 10 modified and 5 added paths, with no deletion, rename or copy. Every path is permitted by the controller's own rules: AGENTS.md, CLAUDE.md and README.md are the _PROPHET_EDITABLE_FILES of tools/workflow/core.py:498-506; docs/implement/DOCUMENTATION_INTEGRITY_AUDIT.md, docs/implement/README.md, docs/spec/README.md, docs/spec/architecture/ARCHITECTURE.md and docs/spec/architecture/adr/ADR-008-document-precedence.md are inside the _PROPHET_EDITABLE_PREFIXES of core.py:507 (docs/spec/, docs/implement/); the two phase READMEs are allowed by the todo/phases/*/README.md branch of _prophet_path_allowed (core.py:515-517); todo/phases/P00-engineering-baseline/T005.md and T006.md are new contract paths (status A) allowed by core.py:518-519; todo/config.yaml is the one config path the layer may write. I re-ran core.py's own _prophet_path_allowed over the entire changed set and it returns allowed for every path. The two remaining added paths, todo/amendments/A0008/request.json and prophet-001.json, are written by the controller itself after its scope check (core.py:1586-1589), so their presence in the candidate is by construction. Nothing under tools/workflow/, .claude/, todo/schemas/, .github/, src/ or tests/ is created, modified or deleted.

FROZEN STATE (pass). Parsing both configs: the only top-level difference is spec_revision ('v1-2026-09-19-research-threats-and-strategy-extension' -> 'v1-2026-09-19-documentation-integrity'); intent_revision is unchanged; tasks go from 67 to 69 with exactly T005 and T006 added and none removed; no pre-existing task entry differs in any field (status, attempt, base/candidate/approved commit, latest_review, depends_on, phase, task_file, superseded_by). I also ran the controller's own _without_prophet_owned_config_fields over candidate against base (core.py:469-487): equal, so the mechanical freeze the finish gate applied is reproduced independently.

EXISTING CONTRACTS (pass). I compared the blob of every todo/phases/**/T[0-9][0-9][0-9].md that exists at the base commit against the candidate: all 67 are byte-identical; the only contract paths that differ are the two new ones. No APPROVED contract gained an obligation, so no approved_commit stopped describing what was reviewed.

NO GATE WEAKENED (pass). The change deletes 29 lines in total and I read every one: 3 in AGENTS.md, 4 in CLAUDE.md, 1 in README.md, 2 in docs/spec/README.md, 13 in ARCHITECTURE.md, 5 in ADR-008, 1 in todo/config.yaml. No 'Must not' clause, acceptance criterion, test assertion, safety, risk, promotion or precision rule is removed or relaxed anywhere; no test file, CI file or the exception registry docs/implement/ci/suppressions.toml is touched (no suppression was added to quiet a finding); T005 and T006 only add checks and only add prohibitions. The ADR-008 insertion preserves the existing rules verbatim and explicitly forbids the new task-contract tier from relaxing a Must not clause, an acceptance criterion, or a safety, risk, promotion or precision rule set by a higher tier.

COHERENCE (fail, two findings). Against that, most cited clauses do check out, and I verified them rather than accepting them: the seven specification documents T005 names all exist, todo/README.md does have a section 5 reference catalogue, todo/schemas/triage-result.schema.json does contain SPEC_DEFECT and core.py:2437-2438 does route SPEC_DEFECT to PLANNING, so the docs/spec/README.md replacement of the non-existent SPEC_BLOCKED state is correct; core.py:89-108 MAINTENANCE_FORBIDDEN_PREFIXES/MAINTENANCE_FORBIDDEN_FILES and todo/maintenance/M0001 and M0002 allowed_paths match the new AGENTS.md/CLAUDE.md wording about the maintenance lane and the dependency manifests; T001's 33d28ab added requirements.lock.txt and T031's 7ee2c91 modified pyproject.toml, so 'only a Developer whose task contract requires it' describes observed practice; ADR-006 does enumerate the tier order T006 restates, does place application/orchestration above strategy, risk and execution and below presentation, and does reserve a platform layer for a new ADR; ADR-009:53-56, .claude/agents/planner.md:78-81, src/robinhood_lp/qualification/hook_pack.py:27 and ARCHITECTURE.md:189 ('Each ADR is immutable once accepted') all say what the audit claims; every Txxx cited anywhere in the documentation resolves to a configured task, every G-/T-/R- identifier resolves in its owning document, and all 11 phase README task lists equal the phase sets in todo/config.yaml; I recomputed ARCHITECTURE.json 2.2's dotted names and got exactly 17 unresolvable ones, all on PLANNED-task rows, matching the audit's claim, with no APPROVED row unresolvable on existence grounds; README.md and CLAUDE.md name T039, which todo/config.yaml records as T038's superseded_by successor, and ARCHITECTURE section 5 now names T027 (T023's successor) and T043. The two failures are: (1) the legend this change added to ARCHITECTURE.md section 2.2 (lines 153-158) asserts that every APPROVED row names what that task delivered, and three rows it kept falsify that assertion (T021, T027, T039 - detail in required_changes); (2) todo/WORKFLOW.md:175-179, which this change did not touch but which is live, PROPHET-writable and the controller manual the Manager follows, still states the exact rule the change declares false in AGENTS.md and CLAUDE.md, and the audit's residual register does not record it.

PLAN VALIDITY (pass). python -m tools.workflow validate reports status OK; I also ran it under /usr/bin/python3 with the same result. The set of configured contract paths equals the set of contract files on disk (69 = 69, no orphans in either direction, no duplicate task_file); the dependency graph is acyclic; T005 and T006 each begin with their own task id, contain the six required sections exactly once, and name exactly their depends_on list ['T003'] in the Dependencies section; tests/test_workflow_contracts.py, tests/test_documentation_links.py, tests/test_config.py and tests/test_workflow.py pass 109/109 with PYTHONPATH=src, and git diff --check is clean.

PROVENANCE AND SECRETS (pass, with imprecisions noted). Every mutable fact the change states is internal to this repository and I reproduced the ones that matter: base and main HEAD are both 02c8f16; 69 tasks; 11 phase READMEs consistent with the config; 17 planned paths in section 2.2; the CI workflow has exactly the quality, lockfile-integrity and network-tests jobs; tests/test_documentation_links.py resolves only relative Markdown links and has an explicit can-it-fail test; the prophet's per-file change counts (AGENTS +7/-3, CLAUDE +11/-4, ARCHITECTURE +27/-13, ADR-008 +17/-5, config +27/-1, T005 80 lines, T006 76 lines, audit 147 lines) and its 101/29 split are accurate. No added line contains a URL, API key, authorization header, keystore path or environment value. Non-actionable imprecisions I resolved rather than left open: the three manifest names include requirements.in, which is absent from the repository but is listed in core.py:104 and was mandated by the Owner's ruling, so I do not require a change; the audit's 'ADR-008:78' matches the candidate file's line number while the base defective line is 71, and it transcribes the Owner's ruling; the audit's section 1 calls T039's row one that 'points at a real module' although it names none.

Verdict: FAIL. The defects are documentation-accuracy and record-keeping defects inside files this change already edits, not a scope, freeze, contract, gate, plan or secret violation; the repair is a bounded second pass by the prophet on the same layer.

## Required changes

- docs/spec/architecture/ARCHITECTURE.md section 2.2: the legend this change added at lines 153-158 ('A row whose task is APPROVED names the module, module set or artifact that task delivered, and every path it names exists in this repository') is contradicted by three rows the change kept, so the sentence is false as written about the table it describes. T021's row (line 98) names `robinhood_lp.protocol.abi`, which T010 delivered: src/robinhood_lp/protocol/abi.py says 'Canonical ABI encoding and PoolId derivation for V4 PoolKey (T010)' in its own docstring and was added by e6ed1f3 'feat(phase-1): T010 canonical protocol identifiers', whereas T021 (06e7b48d..ec391edc) delivered src/robinhood_lp/protocol/abi_artifacts.py, docs/implement/protocol-artifacts/v4-core-e50237c.json, tools/oracle/src/SelectorOracle.sol and their tests. T027's row (line 138) names `robinhood_lp.discovery.eligibility`, which T023 delivered (5deda6f 'feat(phase-2): T023 pool and hook eligibility classifier'); git log over that file lists no t027 commit at all, while T027 delivered src/robinhood_lp/discovery/research_classification.py (671ff8c, a0686f7). The section therefore names the same module twice, once for the retired T023 and again for its APPROVED successor T027, and T027's own delivered module appears nowhere in the table. T039's APPROVED row (line 139) names no module at all although T039 delivered robinhood_lp.qualification.per_pool_window, per_pool_coverage and per_pool_window_runbook (363823e6..90ecfcb0). Either correct those rows to the modules their tasks delivered or narrow the legend so it states only what is true (for instance that an APPROVED row's named paths exist in the repository), and adjust the audit's section 1 attribution ('the remaining APPROVED rows already point at real modules ... T039 ...') to match. ARCHITECTURE.md is already in this change's edited surface, so the repair stays inside the Owner's scope constraint.
- todo/WORKFLOW.md lines 175-179 still state the rule this change declares false: '... `.github/`, `src/`, `tests/` and the dependency manifests are in no layer's scope at all. They are changed only by an explicit bootstrap act ...'. The text this change added to AGENTS.md (lines 49-54) and CLAUDE.md (lines 61-70) says the opposite - that `src/`, `tests/` and `.github/` are the working surface of an ordinary development task, that only the dependency manifests are out of reach of every amendment layer and the maintenance lane, and that the maintenance lane may edit only paths its record declares in advance. WORKFLOW.md is PROPHET-writable (core.py:498-506), is the manual the Manager follows when it decides what a route may touch, and its sentence is the third instance of exactly the claim the Owner's direction item 2 called false; ADR-008's own rule 2 makes a contradiction between two documents of the same (unranked) precedence a bug to be resolved by the document owner. The change's residual register records the other same-class leftovers (section 4 item 4) but not this one. Either record it in docs/implement/DOCUMENTATION_INTEGRITY_AUDIT.md sections 4 and 5 as a found-but-unfixed item with the reason it is deferred, or, if the Owner's Q1 'fix them all together' ruling is read as covering this third instance, correct the sentence; what must not remain is a repository in which the Manager's own manual asserts the opposite of AGENTS.md and CLAUDE.md with no record anywhere.

## Unknowns

- None.
