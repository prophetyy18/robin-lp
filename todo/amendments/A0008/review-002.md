# A0008 owner amendment review

- Base commit: `af542b852bc588d77d1dc5b83f9487bbef330d9f`
- Candidate commit: `433694a923c10f397dc5ecc1c07114ad110a223f`
- Verdict: **PASS**

## Summary

Independent PROPHET-change review of A0008 attempt 002 (layer PROPHET, task_ids [], target_status null), the bounded repair round after review-001 judged FAIL. Identity: the controller's runtime record /home/lpdev/lp/.git/robinhood-lp-workflow/A0008.json gives attempt 2, layer PROPHET, base af542b852bc588d77d1dc5b83f9487bbef330d9f and candidate 433694a923c10f397dc5ecc1c07114ad110a223f, and the review worktree is clean at exactly that candidate. The change touches three documents plus the amendment result record the controller writes itself; all seven checks pass.

SCOPE (pass). git diff --name-status base..candidate reports three modifications (docs/spec/architecture/ARCHITECTURE.md, todo/WORKFLOW.md, docs/implement/DOCUMENTATION_INTEGRITY_AUDIT.md) and one addition (todo/amendments/A0008/prophet-002.json), with no deletion, rename or copy. I re-ran core.py's own _prophet_path_allowed over the changed set (core.py:498-520): the two docs paths are inside _PROPHET_EDITABLE_PREFIXES and todo/WORKFLOW.md is one of _PROPHET_EDITABLE_FILES, so all three are allowed; the fourth path is written by the controller after its own scope check (core.py:1586-1589), so its presence in the candidate is by construction. Nothing under tools/workflow/, .claude/, todo/schemas/, .github/, src/ or tests/ is created, modified or deleted, and no todo/phases/ path appears in the diff at all.

FROZEN STATE (pass). todo/config.yaml is byte-identical on both sides (blob cfcd5cdb9a2a3d1056424a741cc2dbe26199e5bd), so no pre-existing task differs in status, attempt, commit SHA, evidence, review pointer, dependency, phase or contract path; spec_revision stays v1-2026-09-19-documentation-integrity and intent_revision stays v1-2026-09-19-research-universe-numeraire; both sides carry 69 tasks. I parsed both configs and compared the task maps field by field: equal.

EXISTING CONTRACTS (pass). No path under todo/phases/ is in the diff; both sides hold 69 contract files and every file that existed at base is byte-identical, including the T005 and T006 contracts attempt 001 created. No APPROVED contract gained an obligation, so no approved_commit stopped describing what was reviewed.

NO GATE WEAKENED (pass). The removal at todo/WORKFLOW.md:175-179 is the false prohibition the recorded Owner direction item 2 called out (it lumped src/, tests/ and .github/ together with tools/workflow/, .claude/ and todo/schemas/ as outside every scope). The replacement keeps and sharpens the prohibition on the three gate paths ('in no role's and no layer's scope at all: only an explicit bootstrap act changes them') with the original rationale, and its three statements match AGENTS.md:49-55 and CLAUDE.md:61-70. The controller's enforcement sets are untouched (PROTECTED_PREFIXES, MAINTENANCE_FORBIDDEN_PREFIXES and MAINTENANCE_FORBIDDEN_FILES, core.py:80-108, still forbid .github/ and the manifests to the maintenance lane), and no Must not clause, acceptance criterion, test assertion, or safety, risk, promotion or precision rule is removed or relaxed anywhere; no test, CI or suppression file is touched. The legend this change is built around (ARCHITECTURE.md:153-158) is byte-identical to base, so it was not narrowed to avoid the repair.

COHERENCE (pass). I verified rather than accepted each claim the change rests on. ARCHITECTURE.md section 2.2: the three corrected rows now name what those tasks actually delivered. T021 (config range 06e7b48d..ec391edc) delivered src/robinhood_lp/protocol/abi_artifacts.py (docstring 'Pinned V4 PoolManager protocol artifacts (T021)'), docs/implement/protocol-artifacts/v4-core-e50237c.json and tools/oracle/src/SelectorOracle.sol, while the row's former robinhood_lp.protocol.abi is T010's (docstring T010, added by e6ed1f3). T027 (17dfe99d..a0686f70) added src/robinhood_lp/discovery/research_classification.py, while the former robinhood_lp.discovery.eligibility is T023's (docstring T023, added by 5deda6f). T039 (363823e6..90ecfcb0) added per_pool_window.py, per_pool_coverage.py and per_pool_window_runbook.py; its contract names no layer matrix and no ADR assigns it one, and its new 'presentation / reports' cell follows the legend's own mapping sentence at lines 156-158 and the T036/T038/T043 rows. I re-ran that legend's assertion over the whole table independently: 46 named objects on APPROVED rows, each one existing and attributable to its own row's task, 0 failures; the only unresolved names are the 17 planned paths on PLANNED rows, exactly as audit section 4 item 5 states. WORKFLOW.md's new paragraph is the same three statements as AGENTS.md:49-55 and CLAUDE.md:61-70 and agrees with the maintenance section's own rule (WORKFLOW.md:273-278) and with core.py:89-108; the cited controller lines resolve (_PROPHET_EDITABLE_FILES at core.py:498-506 contains todo/WORKFLOW.md). The audit's new section 4 item 8 quote is accurate: .claude/agents/prophet.md:56-58 does carry the last copy of the claim, and a grep over AGENTS.md, CLAUDE.md, README.md, docs/, todo/, .claude/, tools/, todo/schemas/ and .github/ finds no other copy, so the residual register is complete. Two wording imprecisions I resolved rather than left open, neither of them a contradiction of a live goal, ADR, spec clause or contract obligation: the rationale cites the maintenance lane's pre-declared-path rule as 'WORKFLOW.md 267-272', which in the candidate is the section heading and intro (the bullets are at 273-278; the same content sat at 267-270 in the base numbering) - the cited section is the right one and says what the change claims; and the audit's status note shorthand 'section 1's table, three rows (T021 / T027 / T039)' means the ARCHITECTURE section 2.2 table treated in that section, not the audit's own nine-row table, which the following sentence makes unambiguous. The audit's unchanged provenance line still names base 02c8f16 as the audit-time baseline; it is framed as 'at audit time' and is historical metadata, unchanged by this round.

PLAN VALIDITY (pass). python -m tools.workflow validate returns {"status": "OK"}; the 69 configured contract paths equal the 69 todo/phases/**/T[0-9][0-9][0-9].md files on disk with no orphan in either direction and no duplicate task_file; I recomputed the dependency graph and it is acyclic; the candidate creates no new task contract, so the new-contract formatting checks do not newly apply and were satisfied when attempt 001 added T005 and T006. tests/test_workflow_contracts.py, tests/test_documentation_links.py, tests/test_config.py and tests/test_workflow.py pass 109/109 under the project interpreter with PYTHONPATH=src, and git diff --check is clean; I reproduced both.

PROVENANCE AND SECRETS (pass). Every fact the change newly states is internal to this repository and carries its source: the three corrected rows carry commit ranges and docstring attributions, the WORKFLOW.md row carries its file and the core.py line range that permits it, and item 8 carries the file and line range of the copy it registers. The one non-reproducible element is the supplementary Owner ruling quoted at length in todo/amendments/A0008/prophet-002.json (relayed by the Manager and recorded there); its substance is independently checkable and correct - todo/WORKFLOW.md:175-179 was the third instance of the claim the recorded Owner direction item 2 calls false, review-001's required change 2 expressly allowed correcting it if that ruling covered it, and the path is inside PROPHET's writable set - so I resolved it against the record rather than leaving it open. No added line contains a URL, API key, authorization header, keystore path or environment value (the single case-insensitive 'secret' hit among the added lines is the phrase 'provenance and secrets' naming this check), and no external claim needed a web lookup. The amendment result record validates against todo/schemas/amendment-result.schema.json.

Verdict: PASS. Both of review-001's required changes are made in substance and recorded, the repair stayed inside the surface it had already been authorised to edit, and the audit record's attribution corrections match the repository.

## Required changes

- None.

## Unknowns

- None.
