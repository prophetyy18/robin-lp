# A0007 owner amendment review

- Base commit: `66eba84a25495045f4d1c43365419d1a4356c51f`
- Candidate commit: `4025609ac0299552a5d7e942b82ce8baf71f525a`
- Verdict: **PASS**

## Summary

Independent PROPHET-change review of A0007 (layer PROPHET, task_ids [], target_status null), which deletes the hand-maintained `最后更新：YYYY-MM-DD` header line from the 8 documents under docs/ that carry one and rewrites the amendment-layer paragraph of todo/WORKFLOW.md. Identity: candidate 4025609ac0299552a5d7e942b82ce8baf71f525a is a single commit whose only parent is base 66eba84a25495045f4d1c43365419d1a4356c51f (`git log -1 --format='%H %P'`), the review worktree is clean at exactly that SHA, and branch amendment/a0007-attempt-001 points at it. Scope: `git diff --name-status` reports 9 modified and 2 added paths, with no deletion, rename or copy. The 9 modified paths are docs/implement/TRACEABILITY.md, docs/intent/PROJECT_GOALS.md, docs/spec/operations/OPERATOR_CONTROL.md, docs/spec/product/ASSET_ADMISSION.md, docs/spec/product/WEB_CONSOLE.md, docs/spec/research/DATASET_AND_EVALUATION.md, docs/spec/strategy/LP_METRICS.md, docs/spec/strategy/STRATEGY_ECONOMICS.md (all under the PROPHET-writable prefixes `docs/spec/`, `docs/intent/`, `docs/implement/` at tools/workflow/core.py:507) and todo/WORKFLOW.md (one of the five PROPHET-editable files at core.py:498-506); the 2 added paths are the controller-written amendment records todo/amendments/A0007/request.json and prophet-001.json, which the controller writes itself after its own scope check (core.py:1575-1578) and which no author role can write (todo/amendments/ is in no layer's scope). Candidate request.json is byte-identical to the runtime copy /home/lpdev/lp/.git/robinhood-lp-workflow/A0007-request.json (both md5 7af51685296a3ec654acd4fc36514aea), and prophet-001.json validates against todo/schemas/amendment-result.schema.json. No path under tools/workflow/, .claude/, todo/schemas/, .github/, src/ or tests/ is created, modified or deleted; no task contract is created or edited. Frozen state, confirmed rather than assumed: todo/config.yaml is byte-identical at both commits (blob bd78f4cf39b38653e3346945611c49c2cdf16318), so no pre-existing task's status, attempt, commits, evidence pointer, review pointer, dependency, phase or contract path moved, no task was added (67 tasks at both commits) and both revisions still read v1-2026-09-19-research-universe-numeraire and v1-2026-09-19-research-threats-and-strategy-extension. I re-ran the controller's own PROPHET rule (`_without_prophet_owned_config_fields`, core.py:469-487): the frozen-set comparison over candidate against base config returns equal, so the mechanical enforcement the finish gate applied is reproduced independently. Existing contracts are byte-identical: the todo/phases tree is cf545b9bab6eb0e1017d08d61d941f85cd04e663 at base and at candidate, so no APPROVED contract gained an obligation and no approved_commit stopped describing what was reviewed. No gate weakened: each of the 8 document edits is a pure one-line deletion (git diff --numstat reports 0 insertions and 1 deletion in each; 8 hunks across 8 files, one per file, and `git diff --check` is clean), leaving each 状态 line, the blank line after it, the heading and every other character byte-identical, so the `4-line header, heading + 状态 + blank + section 1` shape becomes heading + 状态 + blank + section 1 with no doubled blank line. Nothing under tools/, tests/ or .github/ reads the deleted string (repo-wide `git grep` for `最后更新` at base returns only the 8 documents plus the A0006 records that quote it; a search for `状态` in tools/, tests/ and .github/ returns zero hits), so no test assertion or check was dropped and no test reads the changed files at all except tests/test_workflow_contracts.py, which only scans for forbidden path substrings. The WORKFLOW.md paragraph is a single hunk and the rest of the file is untouched: the two sentences about CONTRACT and SPEC are preserved verbatim, and the three replaced statements are the retired INTENT clause, `while the target tasks remain PLANNED` and `with a fresh Planner`. No Must-not clause, acceptance criterion or safety, risk, promotion or precision rule was softened - reversing the change restores the assertion that a PASS leaves targets PLANNED, which the new wording `without changing any target's status` still entails for CONTRACT/SPEC targets (they must be PLANNED at prepare, core.py:1452-1460, and the drift check freezes status for every layer) and which is also true for SUPERSEDE targets (APPROVED, frozen) and vacuous for PROPHET (no target). Coherence, every citation checked against the code rather than accepted: AMENDMENT_LAYERS is frozenset({CONTRACT, SPEC, PROPHET, SUPERSEDE}) at core.py:36 and INTENT appears nowhere in tools/workflow/ (verified by grep); the non-PROPHET layers require at least one target (1446-1447) and the eight-task cap (1448), single-live-amendment rule (1425-1426) and unfinished-task gate (1421-1424) are as cited; PROPHET refuses --task and is the only layer allowed an empty target set (1441-1445, request target_status null at 1486); the PROPHET writable surface and its two bounds (new-contract-only, deletion and modification refused) are exactly _prophet_path_allowed and its companions (493-520), and the config drift test frees only the two revisions and the tasks the change adds (469-487, 1554-1568); SUPERSEDE targets APPROVED tasks and validates superseded_by to name an existing task other than itself (684-696) while the target contract text stays writable (allowed_contracts, 1544); a PASS is `git merge --ff-only record.branch` after the review state is APPROVED (1703) with no status change permitted anywhere; and the retry gate accepts CHANGES_REQUESTED or resolved BLOCKED, keeps layer and task_ids, and rebases on the retained candidate (1750-1777), returning agent `planner` for every layer including PROPHET (1778) while the initial PROPHET author is the `prophet` agent (1495-1501) and the record is written as prophet-<attempt>.json (1577-1578). The rewritten paragraph therefore says only what those branches permit, and it contradicts no live goal, no accepted ADR (none is in the diff; all 14 ADRs with `date:` front matter are untouched, as is ADR-009's `> Date:` form), no specification clause and no live contract obligation - no document, test or tool requires the deleted line, and docs/README.md plus the three layer READMEs define no header convention. Two immaterial imprecisions I found and judged non-actionable: the rationale attaches `(core.py:460-461)` to the phrase `plus the target contract text`, whereas 460-461 is the SUPERSEDE superseded_by field pop and the contract-text permission is at 1544 (the claim itself is true, and the same rationale cites 1544-1546 correctly for the same permission), and the record says the paragraph is `now lines 99-111` where the paragraph runs 99-110 and 111 is its blank separator line. One disclosed residual outside this change's authority, consistent with the change's own unresolved_questions: .claude/agents/planner.md:79 and :81 still describe an amendment as targeting `multiple still-PLANNED task contracts` within the `CONTRACT`, `SPEC`, or `INTENT` layer. That text was already stale against AMENDMENT_LAYERS before this candidate, .claude/ is outside every role's scope, and this change removes the last other place that repeated the retired name, so it strictly reduces the contradiction rather than creating one; the A0006 review had recorded the WORKFLOW.md half of it as an out-of-scope observation, and A0007 repairs exactly that half. Plan validity: `python -m tools.workflow validate` reports status OK (reproduced with both the conda interpreter and /usr/bin/python3); the 67 configured contract paths equal the 67 contract files on disk; the dependency graph is acyclic; every contract starts with its task id and carries the six required sections exactly once with a Dependencies section equal to its depends_on list. No new contract was added, so the new-contract checks are not applicable. tests/test_workflow_contracts.py, tests/test_workflow.py and tests/test_config.py pass 104/104 with PYTHONPATH=src. Provenance and secrets: every newly stated fact is internal to this repository and I reproduced each one - the 8-file set equals `grep -rn '^最后更新：' docs/` exactly, one deleted line per file, 11 insertions and 16 deletions across the 9 authored files (33/16 including the two controller-written records); the stale-date measurement is accurate as recorded, namely 2 stale of 8 against git rather than the 4 the Owner direction claimed (STRATEGY_ECONOMICS.md stamped 2026-09-14, last changed 2026-09-19 by c7a788d; PROJECT_GOALS.md stamped 2026-09-16, last changed 2026-09-19 by 9d6c1b1; OPERATOR_CONTROL.md and ASSET_ADMISSION.md stamped 2026-09-14 and last changed 2026-09-14 by 24c0790, so their stamps were still accurate; the remaining four were stamped 2026-09-19 and last changed 2026-09-19), so the direction's false figure is recorded in unresolved_questions rather than reproduced in any document, which is what the direction required; the 14-ADR count, the absence of INTENT from tools/workflow/, the planner.md line references and the todo/config.yaml lines 5-6 revision fields all check out. No external or web-sourced claim is made, so no WebSearch or WebFetch was needed. No credential-bearing URL, API key, authorization header, keystore path or environment value appears in any changed file; the only scan hit is the pre-existing unchanged heading `# V1 Token 与 Pool 准入要求` in ASSET_ADMISSION.md context. Verdict PASS.

## Required changes

- None.

## Unknowns

- None.
