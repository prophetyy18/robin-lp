# A0032 owner amendment review

- Base commit: `048d7f763a7863cc3709147f9e2b6ed37d864548`
- Candidate commit: `6bd6ba9e265c0eb722d790ab55b630532cc176ed`
- Verdict: **PASS**

## Summary

The candidate is the A0032 lane amendment/a0032-attempt-001, strictly retargeting T084 web-provenance current-evidence language from T109 to T112 at the CONTRACT layer and adding T112 to T084.depends_on in both the contract and todo/config.yaml. The diff against base 048d7f7 is exactly five files: todo/amendments/A0032/impacts.json, todo/amendments/A0032/planner-001.json, todo/amendments/A0032/request.json (the A0032 lane files), todo/config.yaml (single-line addition of "T112" to T084.depends_on, taking it from [T025, T072, T080, T109] to [T025, T072, T080, T109, T112]), and todo/phases/P08-operations-and-web/T084.md (current-evidence references retargeted from T109 to T112 throughout; T109 preserved only as historical read-only predecessor pending SUPERSEDE; pre-T112 qualifier explicitly added in Deliverables and Acceptance). No other T084 field in config.yaml is changed (lifecycle OPEN, claimed false, task_file, attempt=0, base_commit=null, candidate_commit=null, approved_commit=null, latest_review=null all untouched); no other task record in config.yaml is changed; top-level active_task=T112, active_phase=P06, workflow_state=APPROVED, intent_revision=v1-2026-09-21-historical-and-run-replay, spec_revision=v1-2026-09-23-a0026-run-evidence-cutover, agent_runtime, schema_version, project and product_baseline_commit are all byte-identical between base and candidate. T084.md contract header (# T084, phase P08, contract status FROZEN_FROM_BASELINE_6C31778, blank line) is byte-identical between base and candidate. The Must-not clause is byte-identical between base and candidate ("fetch private material, mutate domain state from a read model, hide failed evidence, substitute a free-text, inferred or legacy identity, dataset, revision or evidence binding for the one a manifest records, present a pre-T109 `T105` or `T063` artifact as current provenance, duplicate a market/run/fee projection, infer authority from replay evidence, or treat Web availability as strategy availability."); no Must-not / safety / execution / signer / credential / risk / paper-promotion clause is weakened, removed or rewritten. Outcome text is byte-identical. Console scope in P08 statement about the P08 page set (T087, T088, T083, T100, T104, T088/T103) is preserved; only the sentence naming which task delivers the manifests/reports T084 consumes was retargeted from T109 to T112. Acceptance: the four enumerated clauses are all preserved or strengthened — (a) the WEB-GLOBAL-* / read-only WEB-PAGE-* / visualization mapping clause is untouched; (b) the "manifests and reports it reads record" wording is preserved with T109→T112 swap only; (c) the provenance registration clause (registered strategy identity, schema/registry revision, canonical dataset reference/content hash, evidence schema/checksum and replay availability, no free-text/inferred/legacy substitution) is preserved verbatim except the fail-closed reference moves from "current T109 artifact" to "current T112 artifact"; (d) the distinguishable-rendering clause is strengthened from "current, pre-T109 and T063" to "current (T112) and pre-T112 (`T109`, `T105`, `T063`)", explicitly naming T112 as current and adding T105 alongside T109/T063 as pre-T112 legacy predecessors; (e) the fail-closed mismatch check now reads "current T112 artifact"; (f) the no-duplicate-projection, no-evidence-as-authority, no-secret-in-provenance, paper/testnet/live-confusion, raw IDs inspectable, browser-restart clauses are all preserved verbatim. References: the T109 entry is rewritten as "T109 (historical read-only predecessor manifest and evidence pending the SUPERSEDE amendment; predecessor T105/T063 artifacts stay historical)" and a new T112 entry ("T112 (the current registry-bound manifest and paired simulation-evidence source per A0032; the canonical dataset references and simulation evidence that carry the dataset version, member ranges, reporting numeraire and registered identity/schema/registry revision this console displays)") is added; T087/T088, T100 and T103 entries are byte-identical. The new T112 reference carries the per-A0032 attribution, mirroring the A0030 reviewer precedent for the T073 contract. Owner direction in todo/amendments/A0032/request.json is recorded exactly as supplied (CONTRACT layer, target T084 only, exact A0026-prefixed impact ID, empty affected_existing_tasks, extension of depends_on in todo/config.yaml required); the candidate matches it. The planner-001.json impact_assessment is consistent with the actual diff: intent/specification/implementation/data/operations/security are all marked Not applicable with correct reasoning; contracts/dependencies/verification are marked Applicable with reasoning grounded in the recorded evidence. The bootstrap self-impact guard from commit 5a3012f is satisfied: impacts.json raised=[] and the only impact ID in resolved_task_impacts is the A0026-prefixed A0026:T084:repoint-web-provenance-to-t112-when-approved, which is byte-identical to the entry in todo/amendments/A0026/impacts.json raised[*].impact_id. The resolution uses the prefix of the impact being resolved (A0026), not the new amendment's own prefix, and A0032 > A0026 so the strictly-greater-amendment-ID resolution rule (tools/workflow/core.py:1529) applies. Verified by recomputing open impacts in the review worktree: WorkflowManager._unresolved_task_impacts('T084') returns [] at the candidate commit; status shows 8 remaining A0026-prefixed open impacts (T087, T088, T096, T103, T107, T108, T110, T111) — none on T084 — and status_conflicts=[]. Dependability: T112 is DELIVERED (lifecycle DELIVERED, approved_commit=40ad3a250cac583b5fd03228ca69cb23dc7e5cd6, replaces=[T109]); T109 is DELIVERED (lifecycle DELIVERED, approved_commit=c79e4c83debd0dc7e8996afb612c103fcb8b2e97); both remain satisfiable as T084 dependencies, so adding T112 does not strand the task or invalidate the dependency DAG. The controller's _without_amendment_owned_config_fields (tools/workflow/core.py:869) deliberately strips depends_on from the drift check at the CONTRACT layer, so this depends_on edit is permitted and required at this layer; tools.workflow validate at the candidate commit reports status OK. The candidate does not touch tools/workflow/, .claude/, todo/schemas/, docs/intent/, docs/spec/, docs/implement/, src/, tests/, .github/ or any dependency manifest. No secrets, credentials, keystore paths, environment values, signing payloads or external URLs with embedded tokens are introduced anywhere in the diff. The candidate is in the worktree amendment-review-a0032-001 with no further tracked modifications. Precedent alignment: this is the same shape as the prior CONTRACT amendment A0030 against T073, which received PASS at review-002 with the same impact-resolution / depends_on-extension / must-not-preservation pattern.

## Required changes

- None.

## Unknowns

- None.
