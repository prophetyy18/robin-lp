# A0002 owner amendment review

- Base commit: `9284d569b787ab1f6303b0fe743ed98cf18d02db`
- Candidate commit: `098be1a3139fbec487e58543d4394b63ac2a0bc9`
- Verdict: **PASS**

## Summary

Owner amendment A0002 独立复核结论: PASS(SPEC 层,目标 T040/T041/T042/T043,四项均保持 PLANNED、attempt 0)。一、身份与范围:候选提交 098be1a 是基提交 9284d56 的单一子提交,工作树干净、与该提交一致,git diff --check 无空白问题。改动共 10 个文件,逐路径核对全部落在 SPEC 层许可内:四个目标合同 T040-T043.md、todo/config.yaml(depends_on 与 spec_revision)、docs/spec/protocol/PROVIDER_FACTS.md、docs/spec/architecture/ARCHITECTURE.md、新增 docs/spec/architecture/adr/ADR-013-finalized-window-pinning-and-partition-reconciliation.md,以及由 finish-amendment 门本身写入的 todo/amendments/A0002/request.json 与 planner-001.json(这两份记录写入候选提交是既定工作流产物,与 A0001 先例一致)。src/、tests/、tools/、.claude/、todo/evidence、todo/reviews、AGENTS.md、todo/README.md、CLAUDE.md、README.md、P04 phase README、T030-T038 合同均未改动。二、记录方向与层级:todo/amendments/A0002/request.json 与运行时记录 /home/lpdev/lp/.git/robinhood-lp-workflow/A0002-request.json 逐字节一致,记录的 Owner 方向即五段式原文;layer=SPEC,task_ids=[T040,T041,T042,T043],运行时记录显示 status=AWAITING_REVIEW、base/candidate 与本次复核一致。三、配置与工作流状态不变性(程序化逐字段比较):基/候选 config 仅有 4 处差异——spec_revision v1-2026-09-17 -> v1-2026-09-18,以及 T040/T041/T042 的 depends_on;T043 的 depends_on 未变;所有任务的 status、attempt、base/candidate/approved_commit、latest_review 全等,active_task=T036(APPROVED)、active_phase/workflow_state、agent_runtime(provider/model/context_window/fallback)与全部非目标字段逐字节相等,无任何实现或工作流状态改动。四、依赖一致性与路径检查:四份合同 Dependencies 段落与 config depends_on 集合一致(T040 [T012,T030,T034,T037,T038];T041 [T037,T038,T040];T042 [T038,T041];T043 [T023,T042]),tests/test_workflow_contracts.py 的 sorted-set 等式断言通过;所有 References 中出现的任务 id(T023、T034、T037、T038、T040、T041、T042)都存在且状态合法;新增依赖无环(python -m tools.workflow validate 返回 OK);T037/T038 确在基提交存在且 depends_on 分别为 [T031,T036]、[T036,T037],与 Manager 描述一致。五、方向映射逐条核对:T040 新增 Input datasets 段(每池一 data root、单次 replay 不得混池、被取代数据集不是输入也不是已满足的进入条件、不得要求旧的 1,000,001 区块参考范围),既有 ProtocolFeeUpdated 要求与 dynamic-fee 条款原样保留;T041 新增 Input scope 段(逐池、按该池合格窗口与 data root;pool_init_outside_window 的池不重建,不得用部分窗口/后置状态引导/邻池数据/放宽窗口);T042 新增 Comparison scope, routing and bounds 段,命名两池为异构集合(满足 heterogeneous pools 条款而非留给未来来源)、明确历史状态路由(不可服务的端点产生 blocked check,绝不以 latest/未固定高度/另一高度/事后重读替代,并记录 T034 reason code、高度与端点别名)、要求运行前固定并记录三项成本界限(每池比较高度数、逐高度读取清单、logical-call 与 compute-unit 上限;超限停止并报告耗尽而非静默缩减覆盖)、并声明复用 T037 的逐分区 event_index 对 Parquet 对账而不重新实现;T043 新增 Evidence scope 段,定义零 hooked 池情形下的空集证据(逐池 T023 分类结果含观测 hooks 值与分类理由、无 pack 的原因、明确声明未验证 hook 语义,作为独立产物记录),且不放宽任何非零 hooks 池的完整 pack 要求、不把 flag 当语义。四份合同 References 均包含 T037、T038、ADR-010、ADR-012、docs/spec/protocol/PROVIDER_FACTS.md 这一最低集合。六、可核查性判定:T042 的三项界限与 T043 的空集证据形态是可复核的验收面而非散文式承诺——界限必须在运行前固定、由运行记录携带,验收条款以记录的高度数/读取清单/预算上限为判据,超限必须报告耗尽;基础验收的 "multiple heights" 保留了至少多个高度的下限;T043 的空集路径以逐池分类结果、原因与"未验证语义"声明为判据,且仅在 T038 解析出 hooks 值后确定适用路径,评审者可据产物逐项核对。机械比对显示四份合同的 Outcome/Deliverables/Acceptance/Must not 无任何基线 token 丢失,T042 原有验收与 Must not 语义全部保留并加强,无任何验收放松。七、规范层:PROVIDER_FACTS.md 新增带标签的终结性固定与十兆区块窗口规则段(Owner policy 2026-09-18;链事实:参考池 Initialize 54946237;可变值标注为每次运行需重新探测;溯源说明复用文档头的 2026-09-17 检索时间、chain id 4663 与端点半 A/B;未虚构任何 finalized 高度),并新增被取代数据集记录(run-680e65f4a59842d98b1712a45280779d 非合格、含两缺失 Swap 行标识、行数分歧 3,739/3,266 对 3,737/3,264 与 Swap 3,157 对 3,159、根因为 RawPartitionWriter._merge_into_existing_partition 的 index-only 合并——已在候选提交代码 src/robinhood_lp/storage/writer.py 与 src/robinhood_lp/ingestion/runner.py 处核实该缺陷路径确实存在)。ADR-013 为 addendum,正文明确 "It reopens none of them"(不重开 ADR-002/010/011/012),前端信息格式与 ADR-012 一致,已注册进 ARCHITECTURE.md ADR 目录表格;spec_revision 已提升。八、层外不一致项:todo/README.md 第 6 节(T023/T043 与所选 PoolKey 的 hook 语义)与 CLAUDE.md/README.md 的 FLYBRAIN/USDG 示例均未被编辑,planner 的 unresolved_questions 第一条准确报告了两者并要求 Owner 决定与更高层路线,符合方向第 5 节要求。九、四项披露偏差逐项裁定,均成立且不弱化方向:(1) 四份合同 References 增加 ADR-013——ADR-013 正是方向 4(c) 要求创建的新策略归属地,属加强;(2) T042 References 增加 T034——其正文要引用 T034 reason-code 集与 cross_endpoint_sample_missing 先例(该 reason code 已核实存在于 src/robinhood_lp/quality/reason_codes.py),属有据补充,且未挤掉任何指定引用;(3) T043 depends_on 不变——方向仅对 T040/T041/T042 要求改依赖、对 T043 只要求 References,字面正确;T038 的排序经 T042->T038 传递保证(T042 READY/APPROVED 前 T038 必须 APPROVED),无功能缺口;(4) 保留 FROZEN_FROM_BASELINE_6C31778 标记——方向要求保留四任务身份,基提交 T040 本就同时带有 2026-09-16 Owner amendment 注记与该标记,先例一致,且工具链不要求更改。十、验证证据:python -m tools.workflow validate 返回 OK;tests/test_workflow_contracts.py 4 项全部通过(含 Dependencies 段落与 config depends_on 一致性断言);请求记录与运行时记录逐字节 diff 一致;新增/修改文本中不含任何凭据 URL、API key、授权头或环境值。十一、明确标注的非发现(explicitly not findings,不构成本次 required change,也不阻断本次通过;同类事项按 A0001 先例记于 summary):(1) 两缺失事件以 block number+tx hash+log index 记录,未直接写入规范 EventKey 四元组中的 block hash;该身份在链上唯一,复查时可按 finalized 区块号补齐,且与基提交 T037 合同所用标识一致,方向(4b)的意图(后来读者不会误认为合格输入)已达成,故不属于本候选必须修正的缺陷;(2) T043 未把 T038 列为其直接 depends_on(方向仅要求 T043 的 References),排序由 T042 -> T038 传递保证,不存在可执行性缺口,如需直接边属后续 Owner 决定;(3) 参考池 ZZZ/USDG 在本次修订后存在 T036 与 T038 两代数据集,是否在 T036 文本中正式把旧一代降级为历史证据,方向明确禁止本层编辑 T030-T038,planner 已在 unresolved_questions 中交回 Owner,属 Owner 后续决定。以上三项均为交回 Owner 的观察,本候选提交无需为此改动。

## Required changes

- None.

## Unknowns

- None.
