# A0015 owner amendment review

- Base commit: `a076166cd5d00b6d32cb11f32dc5120f6818ee82`
- Candidate commit: `30d6cc4635b3f2858dde997c03de07c005297e18`
- Verdict: **PASS**

## Summary

Independent Owner-amendment review of A0015 (layer CONTRACT; target tasks T068, T069). Identity: candidate 30d6cc4635b3f2858dde997c03de07c005297e18 is a single commit whose only parent is the stated base a076166cd5d00b6d32cb11f32dc5120f6818ee82 (git rev-list --count = 1, git log -1 %P = a076166), the detached review worktree is clean at exactly that SHA, git diff --check is clean, and the committed todo/amendments/A0015/request.json is byte-identical (cmp) to the runtime record /home/lpdev/lp/.git/robinhood-lp-workflow/A0015-request.json, whose runtime A0015.json records AWAITING_REVIEW attempt 1, layer CONTRACT, task_ids [T068, T069], base a076166 and candidate 30d6cc4. RECORDED DIRECTION: the direction names exactly T068 and T069, and every directed item is implemented, item by item. T068: T063 is removed from its Dependencies (now T060, T062, T065) and from todo/config.yaml; T105 is deliberately not added (T105.md line 8 declares T063, T068, and config confirms), so the T068-T105 cycle the direction guards against does not exist; T068 is kept as the registry producer with parameter schema, version and code provenance, and its manifest-facing role is re-cut as "the deterministic validation surface the manifest authority T105 binds published manifests to"; current manifest binding, the cutover from the hard-coded five-name set and its integration acceptance are moved to T105 (T105.md Deliverables lines 24-30, Replacement and migration lines 40-68); Must not now forbids "publish or validate a manifest", so no second manifest publisher exists; and the false T060 parameter-schema citation is gone - T060.md contains no parameter-schema vocabulary (grep finds none), while T065.md line 20 declares the "immutable parameter schema" the amended References now cite, closing A0013:T068:legacy. T069: T063 is replaced by T105 in Dependencies (T041, T053, T061, T068, T105), in todo/config.yaml and in every normative reference - no T063 string remains in either target contract (grep exit 1); the product-run versus rerun-manifest boundary is stated explicitly ("T069 alone owns the durable product-run lifecycle..."; the T105 artifact rerun acquires no T069 run state; a product rerun creates a new durable run linked to a source manifest kept read-only); Must not adds the matching three prohibitions; and acceptance now covers a new run, a linked rerun whose source manifest stays byte-identical, the artifact command staying artifact-only, cancellation and failure publishing nothing, and fail-closed rejection of legacy or unregistered inputs - closing A0013:T069:legacy. DEPENDENCY CONSISTENCY: each contract's Dependencies line equals its configured depends_on list (T068 [T060,T062,T065]; T069 [T041,T053,T061,T068,T105]); an independent DFS over all 83 configured tasks finds no cycle; and the dependency-edge effects claimed in the impact assessment are accurate - T063's PLANNED consumers are T073, T084, T101 and successor T105 (so it is not retirable here), T068's consumers are T069, T087, T105-T108, and T069's are T087 and T088. CONFIG-FIELD BOUNDARY: a field-for-field diff of base and candidate todo/config.yaml shows the only changed task entries are T068 and T069, each differing only in depends_on; no status, attempt, approved_commit, evidence, replaces/superseded_by, task set, active_task, workflow_state, intent_revision or spec_revision moved. Both targets were PLANNED at attempt 0 at the base and remain so. NO IMPLEMENTATION OR WORKFLOW-STATE CHANGE: git diff --name-status reports exactly six paths - the two target contracts (M), todo/config.yaml (M) and the three controller-written records todo/amendments/A0015/{request,planner-001,impacts}.json (A); nothing under src/, tests/, tools/, .github/, .claude/, todo/schemas/, docs/ or any other contract changed, and no evidence or approval record was created or altered. IMPACT IDS INDEPENDENTLY CHECKED: recomputing the open-impact register from all todo/amendments/*/impacts.json shows exactly two open impacts on each target at base (A0013:T068:legacy, A0014:T068:remove-manifest-reverse-dependency; A0013:T069:legacy, A0014:T069:consume-registry-bound-manifest), and the candidate resolves exactly those four specific IDs, each genuinely closed by the delivered text; the resolution is narrow, not broad. The remaining 24 open impacts (16 A0013 and 8 A0014) are accurately enumerated in the plan rationale and were left open; affected_existing_tasks [] is a defensible claim because T105.md lines 54-59 already require T069 to consume T105 and T068 to cease consuming the manifest, T073/T084/T101 remain its recorded consumers, T087/T088 retain their recorded normative T063 references, and T063/T064/T066/T067 stay on the retirement route. IMPACT ASSESSMENT SPOT CHECKS: G-STRATEGY-REGISTRY-01 is docs/intent/PROJECT_GOALS.md:506 and G-BACKTEST-RUN-01 is :508, both matching the delivered obligations; ARCHITECTURE.md:153 assigns robinhood_lp.strategy.registry to T068 and :155 records T105 as T063's successor; WEB_CONSOLE WEB-PAGE-012 (line 179, owned by T087) is the registered-strategy/run surface cited; ADR-014 lines 99-100 state the per-pool manifest and dataset binding; the retirement implementation claims hold - src/robinhood_lp/reports/manifest.py:118 VALID_STRATEGY_KINDS is exactly the five-name set and src/robinhood_lp/__main__.py carries the rerun-manifest command whose docstring cites the T063 acceptance clause; tools/workflow/core.py:470-483 is indeed the depends_on ownership rule for CONTRACT amendments. ONE AUTHORITATIVE PATH, IMMUTABLE HISTORY: registry identity/parameter validation stays with T068, manifest publication and validation with T105, durable run lifecycle with T069; no historical artifact, APPROVED contract, approval field or prior amendment record was edited, and T105's Replacement and migration already specifies the old code disposition (entry points adapted/replaced, old validator only as unreachable history or an explicitly versioned legacy reader) and read-only preservation of legacy artifacts. ACCEPTANCE PRESERVED: clause-by-clause comparison against base shows no T068 or T069 acceptance criterion, deliverable or Must-not clause dropped or weakened; the changed items are re-pointed (T063 to T105), tightened (registry lookup and rejection moved to the registry surface; Must not extended) and extended with exactly the directed coverage. VERIFICATION RUN (read-only investigation, no planning file edited): python -m tools.workflow validate returns {status: OK}; tools.check_acceptance check reports no findings; tools.check_citations check reports no findings; tools.check_imports check reports no findings; git diff --check is clean; all 83 configured contract paths exist; and with PYTHONPATH=src the planning-related suites pass (tests/test_workflow.py 51 passed; test_workflow_contracts, test_documentation_links, test_documentation_citations, test_import_graph, test_acceptance, test_config, test_layer_direction_t007: 141 passed). Non-blocking observations, none requiring a change: the T068 acceptance line that hands manifest integration to T105 is an assignment rather than a test, which is exactly what the recorded direction ordered and whose verification lives in T105's own acceptance and Replacement and migration; the planner's verification note that "the only changed paths are the two target contracts and the two dependency lists in todo/config.yaml" describes the planner's edits and omits the controller-written amendment records, which finish-amendment adds after its own scope check; and the T068 Must-not "publish or validate a manifest" is slightly broader than A0014's "second manifest publisher" wording while remaining consistent with the Deliverables' registration-query surface. Verdict PASS: the amendment faithfully executes the exact recorded Owner direction, resolves the four named impacts and no others, stays inside the CONTRACT layer's paths and its two dependency fields, preserves every still-valid acceptance standard, keeps config and contract dependencies consistent and acyclic, adds nothing unrelated, and changes no implementation, approval or workflow state.

## Required changes

- None.

## Unknowns

- None.
