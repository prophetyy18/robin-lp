# A0009 owner amendment review

- Base commit: `06f866232f297ab8a8b5502fcf684efd8517e3b9`
- Candidate commit: `be03228f96bb697e39fc2eef01816082c1b42ab7`
- Verdict: **PASS**

## Summary

Independent Owner-amendment review of A0009 (CONTRACT layer; targets T063, T064, T065, T066, T070, T101, T084). Verdict: PASS. Identity and scope: candidate be03228 is the single child of base 06f8662, the detached review worktree is clean at exactly that SHA, and `git diff --check` is clean. The changed path set is exactly the seven target contracts plus `todo/amendments/A0009/request.json` and `planner-001.json`, the two records `finish-amendment` writes after its own path-scope check (same pattern the A0002/A0003 reviews accepted). No `src/`, `tests/`, `tools/`, `.claude/`, `todo/schemas`, `docs/`, evidence, review or dependency-manifest path changed, and `todo/config.yaml` is byte-identical to base, so no status, attempt, commit SHA, evidence pointer, approval datum or dependency field moved; all seven targets are PLANNED at attempt 0. Recorded direction: `todo/amendments/A0009/request.json` is byte-identical to the runtime record `/home/lpdev/lp/.git/robinhood-lp-workflow/A0009-request.json`; the recorded direction, target-task set and layer CONTRACT match the amendment record (AWAITING_REVIEW, attempt 1), the runtime record (base/candidate identical to this review) and the Manager prompt; seven targets is within the eight-task limit, and A0009 is the only active amendment. Contract integrity: for all seven files the Dependencies block, Must not block and Contract status line are byte-identical to base (programmatic section diff), so no dependency edit is needed in config and none was made; the config `depends_on` sets already agree with the contract Dependencies. Item-by-item verification against repository facts. (1) T063: the per-pool manifest resolution matches ADR-014's Consequences (line 98-100: partitions and checkpoints keyed by `(chain_id, contract_address, pool_id)` per row; a run manifest records a single `pool_id` and must be per-pool or widened before a multi-pool run is claimed) and the stated basis (the T100 registry / DS-001 dataset already binds the member pools and each member's own block range, so widening would duplicate it); the panel T101 assembles and T064's pool holdout are selections over a manifest set, consistent with T064's unchanged pool-holdout clause and T101's contract; the unit clause now names both deliverable lists and every recorded quantity; the appended acceptance clause (one manifest per member pool under one run identity; another pool's data fails validation) only adds coverage. (2) T064: the scenario catalogue gives halt/degrade a named home, and `DS-041` is verified as the repository floor (deterministic fallback to the rule baseline, never undefined behaviour or a forced trade); nothing was stretched into a universal fallback rule. The purge/embargo gap is now one length equal to the declared label horizon, derived from it and recorded with the split, matching `DS-022` (a separation band equal to the label horizon, recorded with the split, derivable from the label definition, never hand-picked) and A0003's original one-length framing; Deliverables and Acceptance now agree with the unchanged Must not clause. All other acceptance criteria (point-in-time split boundaries, train/validation/test separation, sensitivity, per-held-out-pool reporting, time-only holdout caveat) are retained. (3) T065: the above-100-percent suppression rule exists in the unchanged Deliverables and in `ECO-REGIME-001` (a complete 5-minute target-token USDG return above 100% produces no risk-increasing candidate, forces in-strategy position reassessment, changes no system run state); only acceptance coverage and the rule's ID were added; Deliverables are byte-identical to base. (4) T066: the lock is now defined as an immutable record written before the test fold is read, freezing candidate version and protocol (parameter-set version, code revision, manifest hash, seed, declared split boundaries, cost assumptions, elimination rules) with content hash, actor and lock time; the experiment harness is named as writer and observer. This matches `DS-031` (evaluation fold seen once and only after the candidate is locked) and STRATEGY_ECONOMICS section 6 (declare split boundaries, costs and elimination rules before seeing the final result; a threshold change creates a new version). Original acceptance criteria are retained and tightened. (5) T070: the minus-80-percent rule exists in the unchanged Deliverables, in `CTRL-MARKET-001` (`return_5m <= -80%` as the emergency breaker outside the strategy model, triggering AUTO_EXIT; an unfinished period must not trigger it) and in `G-EMERGENCY-01`; only acceptance coverage and the two IDs in References were added; Deliverables are byte-identical to base. (6) T101: the retired ten-million-block window is gone from the contract (no occurrence remains), replaced by ADR-015's per-pool window from each pool's own `Initialize` block to the run's agreed finalized block, with ADR-015's measured numbers (reference pool initialized about twelve days before the last probe; measured 0.1008-second block interval; roughly 3.4 thousand five-minute bars or 288 hourly bars); the claim's substance and every sample-size number are retained, ADR-015 joins the References, and the acceptance is untouched. (7) T084: ARCHITECTURE.md section 2.2 maps T084-T086 to `robinhood_lp.web` and T083 to `robinhood_lp.ops.dossier`; T083's contract is the preliminary-paper release dossier with no `WEB-*` requirement, so narrowing 'P08 web-console tasks T083-T086' to 'T084-T086' removes a false classification without dropping coverage. The dataset-version item is resolved on the allowed route and states it: the supplier is T063, already a dependency, whose manifest and reports carry the dataset version, the per-pool block ranges and the reporting numeraire, and the registry-wide question (which datasets exist, what each covers, qualification status) is explicitly scoped to T100's P10 registry query surface, which T100's contract literally delivers; both `WEB-GLOBAL-001` (every page shows the active dataset version and reporting numeraire) and the section 4 provenance bullet (expandable dataset version, member pools with their own block ranges, reporting numeraire and valuation qualification) already require what the amended banner and acceptance clause now reflect, and `WEB-GLOBAL-002` supplies the explicit unknown/absent state used where no dataset stands behind a surface. The new body section follows the T085 precedent added by the passed A0003 amendment, no dependency edge was added or changed, and no retired task (T022, T023, T038) appears in any added contract text. No acceptance criterion, Must not clause, or safety/risk/promotion/precision rule was removed or relaxed anywhere; every edit either adds coverage or makes an existing requirement stricter (derivable -> derived and equal; one declared halt/degrade outcome per catalogue entry with a named reason or fallback). No unrelated requirement was introduced. Evidence: `python -m tools.workflow validate` returns status OK in this worktree; tests/test_workflow_contracts.py 4/4 passed; tests/test_documentation_links.py and tests/test_workflow.py 46/46 passed; the added text contains no URL, credential, key material or environment value. Explicitly noted, non-blocking observations (not required changes and not unknowns: they lie outside the CONTRACT layer this amendment was granted, whose direction states nothing outside the seven contracts and config.yaml may change, and they do not affect any claim in the candidate): (a) `DS-030` in docs/spec/research/DATASET_AND_EVALUATION.md still justifies the deliberately small model families with the retired ten-million-block window; ADR-015 forbids later clauses citing it as the research range, and the amendment record registers this as a separate SPEC-layer amendment. (b) Whether T064's halt/degrade home belongs in the specification rather than the contract is registered for the same later SPEC-layer route; the contract keeps the catalogue it now carries. (c) The planner's result record adds an 'Owner adjudication' paragraph and a post-hoc result-record update; no contract text rests on that adjudication, and the seven contracts and todo/config.yaml are byte-identical to the state the result's summary describes. Both deferred items are correctly outside this candidate's layer and cannot be repaired here without violating the recorded direction, so they do not make this plan defective.

## Required changes

- None.

## Unknowns

- None.
