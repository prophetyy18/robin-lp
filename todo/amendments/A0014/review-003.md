# A0014 owner amendment review

- Base commit: `fc2e6583ea76b4feae7c1fdb27af2f00d334cfb1`
- Candidate commit: `0dd05c1f41152d15a88a23b7bb9d1cf60c3f0494`
- Verdict: **FAIL**

## Summary

Attempt 3 repairs two rounds of review findings and is coherent, but it breaks a required repository gate, so it cannot merge as it stands. What passed. (1) Scope: measured against the amendment's original base 1aa6f45 (freeze base, recorded in review-001.json), every changed path is writable by PROPHET - docs/spec/, docs/implement/, todo/README.md in earlier attempts, the P06 phase README, todo/config.yaml, controller-written A0014 records, and the four contracts T105-T108 that this same amendment created (status A; editable per todo/WORKFLOW.md:195-203 'a contract the same amendment added stays editable (A)' and tests/test_workflow.py:1265 test_prophet_retry_may_repair_a_contract_the_same_amendment_added). No file was deleted and no pre-existing contract was touched: under todo/phases/ only P06/README.md (M) and T105-T108 (A) differ from 1aa6f45, so review-002's requirement to update the successor contracts' migration accounting was met lawfully. (2) Frozen state: running the controller's own _without_prophet_owned_config_fields over base and candidate configs returns two identical objects; only spec_revision differs at top level; 83 tasks, and the configured contract-path set equals the contract files on disk (83/83, no orphans). (3) No gate weakened: the diff deletes nothing but superseded wording - no Must-not clause, acceptance criterion or test assertion is relaxed or dropped, and the TRACEABILITY rows, todo/README.md:326 and the P06 phase list are replaced by supersets that add T105-T108. (4) Coherence: the cited stale sentences resolve exactly - T087:55-56 ('T063 (the manifest and coverage a range selection is read against)'), T088:61-62 ('T063 (the manifest, coverage and attribution checksums...)') and T102:70-71 ('T064 (the robustness and anti-overfitting protocol...)'); VALID_STRATEGY_KINDS exists at src/robinhood_lp/reports/manifest.py:118; the rerun-manifest CLI is at src/robinhood_lp/__main__.py:88; superseded_by is a real config field (todo/schemas/config.schema.json:94); the new contracts start with their task id, carry the six required sections exactly once plus 'Replacement and migration', and each Dependencies section names exactly its depends_on list; 'python -m tools.workflow validate' returns OK, the graph is acyclic, and python -m pytest tests/test_workflow.py tests/test_workflow_contracts.py tests/test_acceptance.py tests/test_documentation_links.py tests/test_documentation_citations.py is 101 passed. (5) Impact ledger verified rather than trusted: T096's closure is 53 tasks and contains T063/T064/T066 and none of T105-T108, exactly as recorded; every ordinary PLANNED consumer of a retiring predecessor now has an impact (T063: T068/T069/T073/T084/T101; T066: T073/T083; T064 and T067 have only their successors T106/T108), and the proposed rewiring stays acyclic and brings all four successors into T096's closure, with T108 reachable from T096 only. resolved_task_impacts=[] is correct: this change edits no existing contract and closes no earlier-recorded A0013 conflict. (6) Staleness: TRACEABILITY.md, ARCHITECTURE, THREAT_MODEL, ADR-003, todo/README.md and the P06 README are all synchronized, and the only surviving references to the stale module paths are inside todo/reviews/ historical records, which must not be edited. The candidate also repairs the five pre-existing citation findings that todo/reviews/P06/T065/review-002.md:244 records as a known failing gate (docs/implement/DOCUMENTATION_INTEGRITY_AUDIT.md:242 member-access plus four architecture-section22 rows), by correcting the module paths rather than by suppressing them. The failure: the same edit that repairs those five findings introduces five new ones in the T006 import-graph gate. ARCHITECTURE.md line 119 (T063 row) now declares the layer 'backtest / presentation / reports', but tools/check_imports/layer_map.py:140 classifies robinhood_lp.reports as 'backtest' and LAYER_ALIASES has no entry for that string, so canonical_layer('backtest / presentation / reports') != classify('robinhood_lp.reports.manifest') etc. Running the checker on both trees shows the base passes ('import-graph check passed: no findings') and the candidate fails with five architecture-disagreement findings at ARCHITECTURE.md:119, and python -m pytest tests/test_import_graph.py fails accordingly. The whole suite is a CI gate (.github/workflows/ci.yml:48 runs pytest as the quality gate), so the candidate leaves the repository red on a gate that was green at the base; the only other suite failure, tests/test_abi_artifacts.py, is pre-existing and environmental (tools/oracle/lib Solidity sources are not present in the worktree). The T105 row at line 155 carries the same non-canonical layer string and is only escaping the check because _TASK.fullmatch rejects the annotated task cell 'T105 (successor to T063)', so the same defect is latent there. A second, minor defect is in the amendment record itself: prophet-003.json's impact_assessment.specification asserts 'Intent and spec_revision remain unchanged' while this same candidate raises spec_revision in todo/config.yaml from v1-2026-09-20-admission-write-boundary-and-threat-owners to v1-2026-09-20-strategy-successor-ownership - the raise itself is correct and permitted (todo/WORKFLOW.md:117-120, 'may change both revisions', and this attempt edits docs/spec/), but the record contradicts its own diff.

## Required changes

- Restore the T006 import-graph gate that this change breaks. ARCHITECTURE.md section 2.2 line 119 (the T063 row) now declares the layer 'backtest / presentation / reports' for the modules robinhood_lp.reports.{manifest,metrics,rerun,run_identity,validation}, but tools/check_imports/layer_map.py:140 classifies robinhood_lp.reports as 'backtest' and the LAYER_ALIASES table at tools/check_imports/layer_map.py:61-76 has no alias for 'backtest / presentation / reports', so canonical_layer(declared) != classify(module) fires five times (tools/check_imports/check.py:372-385). Measured: at the review base fc2e658 the checker and tests/test_import_graph.py::test_check_passes_on_real_repository pass with no findings; at the candidate the checker reports five architecture-disagreement findings (ARCHITECTURE.md:119 for robinhood_lp.reports.manifest, .metrics, .rerun, .run_identity, .validation) and the test fails, and pytest is the CI quality gate (.github/workflows/ci.yml:48). Because tools/check_imports/layer_map.py is outside every role's and every layer's write surface (only a bootstrap act changes tools/), a PROPHET change must declare the layer the map assigns: use a canonicalising string such as 'backtest' or 'backtest / research' (the T064 and T066 rows already use 'backtest / research', which canonicalises to 'backtest'). If a genuine layer reassignment of robinhood_lp.reports to 'presentation / reports' is intended, that is an ADR/bootstrap act on the layer map, not a collateral edit, and must not be smuggled in as a doc change. Apply the same correction to the T105 row at line 155, which carries the identical non-canonical string and is currently skipped only because the checker requires a bare task cell; it would fire the same five findings as soon as the successor row is normalised. Evidence: python -m pytest tests/test_import_graph.py (or the equivalent tools.check_imports run) must report no findings after the fix.
- Correct the amendment record's own description of the specification revision. todo/amendments/A0014/prophet-003.json, impact_assessment.specification, states 'Intent and spec_revision remain unchanged', but candidate 0dd05c1 raises spec_revision in todo/config.yaml from v1-2026-09-20-admission-write-boundary-and-threat-owners to v1-2026-09-20-strategy-successor-ownership, which is correct and permitted for this layer (todo/WORKFLOW.md: 'PROPHET ... may change both revisions') precisely because attempt 3 edits docs/spec/ (ARCHITECTURE.md, ADR-003, THREAT_MODEL.md). State that spec_revision was raised, and why, so the record does not contradict its own diff; intent_revision is genuinely unchanged because docs/intent/ was not touched. No impact-ledger change is required by this item.

## Unknowns

- None.
