# A0006 owner amendment review

- Base commit: `e7e31498594e4815ee5585ae70be75d356dffcf4`
- Candidate commit: `9d6c1b18b91c9767768bdcde3ca528ee8fefbf78`
- Verdict: **PASS**

## Summary

Independent PROPHET-change review of A0006 (layer PROPHET, task_ids empty), which repairs broken relative Markdown links in docs/intent/PROJECT_GOALS.md. Identity: candidate 9d6c1b18b91c9767768bdcde3ca528ee8fefbf78 is a single commit whose only parent is base e7e31498594e4815ee5585ae70be75d356dffcf4, the review worktree is clean at exactly that SHA, and branch amendment/a0006-attempt-001 points at it. Scope: the diff contains exactly three paths - docs/intent/PROJECT_GOALS.md (M, inside the PROPHET-writable docs/intent/ prefix listed in tools/workflow/core.py _PROPHET_EDITABLE_PREFIXES and in the PROPHET section of todo/WORKFLOW.md) plus the two controller-written amendment records todo/amendments/A0006/request.json and prophet-001.json (A; the candidate request.json is byte-identical to the runtime copy /home/lpdev/lp/.git/robinhood-lp-workflow/A0006-request.json, md5 2e5c7f3d8c44c265c2fa4bfc839350b4, and prophet-001.json conforms to todo/schemas/amendment-result.schema.json, matching the A0003/A0004/A0005 candidate pattern). No file is deleted, nothing under tools/workflow/, .claude/, todo/schemas/, .github/, src/ or tests/ is created or modified, and no task contract is added or edited. Frozen state: todo/config.yaml is byte-identical at both commits (blob bd78f4cf39b38653e3346945611c49c2cdf16318), so no pre-existing task's status, attempt, commits, evidence pointer, review pointer, dependency, phase or contract path moved and no task was added. Existing contracts: git diff base..candidate over todo/phases is empty, so no APPROVED contract gained an obligation and no approved_commit stopped describing what was reviewed. No gate weakened: reversing the five target substitutions in the candidate document reproduces the base document character for character (19002 vs 19077 characters; 7 insertions and 7 deletions, every one a link target), so no requirement, Must-not clause, acceptance criterion or sentence outside the link targets changed; git diff --check is clean and the file mode is unchanged. Coherence, resolved rather than assumed: at base the document carried exactly 7 relative links (5 distinct targets) and all 7 were broken; at the candidate all 7 resolve, and a scan of all 179 tracked *.md files finds zero unresolved relative links anywhere (112 relative links checked), independently reproducing the author's 7/5 count and its zero-broken-outside claim. Each new target exists and is the document its sentence describes: ../spec/strategy/STRATEGY_ECONOMICS.md (V1 USDG-denominated LP strategy economics), ../spec/operations/OPERATOR_CONTROL.md (V1 human takeover, pause and exit), ../spec/product/ASSET_ADMISSION.md (V1 token and pool admission), ../spec/product/WEB_CONSOLE.md (V1 web console) and ../implement/TRACEABILITY.md (V1 goal traceability index, listed by docs/implement/README.md and cited as docs/implement/TRACEABILITY.md by the root README). Every other reference in the repository to those five documents uses the same paths (6x docs/spec/strategy/STRATEGY_ECONOMICS.md, 3x docs/spec/operations/OPERATOR_CONTROL.md, 11x docs/spec/product/ASSET_ADMISSION.md, 5x docs/spec/product/WEB_CONSOLE.md); no stale specs/ path survives anywhere outside the forbidden-substring literal in tests/test_workflow_contracts.py; no accepted ADR, specification clause or live contract obligation is contradicted, T025 being the only contract that mentions PROJECT_GOALS.md and only in its References list; and section 10's statement still matches TRACEABILITY.md's own rule that T096 extends the index. The two prose names in section 10 (docs/spec/product/USE_CASES.md, docs/intent/SUCCESS_CRITERIA.md) are byte-identical, are named nowhere else in the repository, own no task and no file, and were correctly left as future-work statements rather than turned into links or deleted. The change is also the direct follow-up to A0005, whose prophet record raised exactly these five links as an Owner question. Plan validity: python3 -m tools.workflow validate reports status OK; the 67 configured contract paths equal the 67 contract files on disk; the dependency graph is acyclic and every dependency names an existing task; every contract starts with its task id, carries the six required sections exactly once, and its Dependencies section equals its depends_on list; tests/test_workflow_contracts.py passes 4/4 and tests/test_workflow.py plus tests/test_config.py plus the contract tests pass 104/104 with PYTHONPATH=src (plain import of robinhood_lp fails only because the environment's editable install points at a removed worktree, a pre-existing host condition). Provenance and secrets: the change restates no external mutable fact - every claim is about this repository at this candidate and carries its file or command source - and I independently reproduced each one (the 7-occurrence/5-target count, every old target missing and every new target present, the one-file 7-insertion/7-deletion diff, zero broken relative links repo-wide, the untouched section 10 prose, no task created, validate OK, the stale header 最后更新：2026-09-16 whose previous change is c7a788d dated 2026-09-19, the test that asserts only literal substrings such as docs/specs/ and so cannot catch ../specs/... or ./ASSET_ADMISSION.md, and the absence of any link-checking tooling), so no WebSearch or WebFetch was required; no credential-bearing URL, API key, authorization header, keystore path or environment value appears in any changed file, the Keystore 密码 text in the diff being pre-existing unchanged context in the goal document. Two out-of-scope observations, neither a defect of this change and neither requiring action here: todo/WORKFLOW.md line 101 still names an INTENT layer that does not exist in AMENDMENT_LAYERS (introduced by f8d83bf on 2026-09-17, untouched by this candidate), and the author's own unresolved questions - whether a link-only repair ought to bump intent_revision, a free-form value nothing else in the repository pins, and whether tasks should be created for the two undeclared section-10 documents - are Owner-preference questions already recorded in prophet-001.json, not inconsistencies this change creates. Verdict PASS.

## Required changes

- None.

## Unknowns

- None.
