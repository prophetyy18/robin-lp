# A0016 owner amendment review

- Base commit: `c19ba065ec9733a1b4bf80e794a07cec5c355745`
- Candidate commit: `22e050a4d63d509f6ebcbc05e784cb96599954dc`
- Verdict: **PASS**

## Summary

Independent Owner-amendment review of A0016 attempt 2 (layer CONTRACT; targets T073, T083, T084, T101). IDENTITY: 22e050a4d63d509f6ebcbc05e784cb96599954dc is a single commit whose only parent is c19ba065ec9733a1b4bf80e794a07cec5c355745 (git log -1 %P), the detached review worktree is clean at exactly that SHA, the runtime record /home/lpdev/lp/.git/robinhood-lp-workflow/A0016.json names AWAITING_REVIEW attempt 2, layer CONTRACT, task_ids [T073, T083, T084, T101], base c19ba06, candidate 22e050a and freeze base b2ee26a, and the committed todo/amendments/A0016/request.json is byte-identical (cmp) to the runtime A0016-request.json. DIRECTION AND BOUNDARY: the recorded direction names exactly those four tasks and declares target_status PLANNED; the attempt diff c19ba06..22e050a is exactly the one-clause repair of todo/phases/P08-operations-and-web/T084.md plus the controller-written todo/amendments/A0016/planner-002.json, and the whole-amendment diff b2ee26a..22e050a is exactly the four target contracts, todo/config.yaml and the controller-written A0016 records (request, impacts, planner-001, review-001 json+md, planner-002) - nothing under src/, tests/, tools/, docs/, .github/, .claude/ or todo/schemas/ changed, so there is no implementation or workflow-state change. A field-for-field comparison of the base and candidate configs shows the only changed task fields are the four depends_on lists - T073 [T025,T027,T105,T107], T083 [T082,T107], T084 [T025,T072,T080,T105], T101 [T050,T069,T100,T104,T105] - and no status, attempt, approved_commit, evidence, replaces, superseded_by, task set, active_phase, active_task, workflow_state, intent_revision or spec_revision moved; all four targets are PLANNED at attempt 0; docs/intent/ and docs/spec/ are byte-identical across the whole amendment. REVIEW-001 REPAIRS, EACH INDEPENDENTLY RE-MEASURED: (1) the T084 clause no longer says the two further P08 web-module rows were recorded 'since A0014'; git blame on docs/spec/architecture/ARCHITECTURE.md:160-161 and git log -S 'robinhood_lp.web.runs' / '-S robinhood_lp.web.backtest_result' attribute both rows to A0012's candidate 2b64df7034965ab78146204e7681008d74a760f6 (confirmed the full SHA of 2b64df7), A0014's candidates 0dd05c1 and bd1ff41 changed ARCHITECTURE.md only by the T105-T108 successor rows (:155-158), the transition paragraph (:171-177) and the layer paragraph (:179-183), and git log over docs/spec/product/WEB_CONSOLE.md lists no A0014 commit, so the corrected paragraph now describes section 2.2 as it stands and correctly places the T087/T088 pages outside T084's acceptance (WEB_CONSOLE.md:188 and :202 state the T087/T088 ownership without changing T084-T086 acceptance scope). (2) The stale line citations in planner-001.json are handled lawfully: that file is byte-identical and a CONTRACT amendment may change only its target contracts and todo/config.yaml (tools/workflow/core.py:1707-1716), so attempt 2 records the re-measured positions in planner-002.json and names the superseded numbers; every re-measured citation checks out in the candidate tree - ARCHITECTURE.md :131 (T084-T086), :142 (T101), :155 (T105), :157 (T107), :159 (T073), :160 (T087), :161 (T088); WEB_CONSOLE.md :58 (WEB-GLOBAL-006), :179/:188 (WEB-PAGE-012/T087), :191/:202 (WEB-PAGE-013/T088); DATASET_AND_EVALUATION.md :28 (DS-002), :106 (DS-031), :156 (DS-043); PROJECT_GOALS.md :506 (G-STRATEGY-REGISTRY-01), :508 (G-BACKTEST-RUN-01), :536 (G-PAPER-AUTHORIZE-01). (3) The occurrence arithmetic is exact: grep -o counts six T063/T066 tokens in T073 (three `T063` and three `T066` at :35-36, :54-55, :68), three `T066` in T083 (:23, :35, :42), four `T063` in T084 (:48, :65, :75, :83) and none in T101 - thirteen total, each occurrence a legacy-only rendering or prohibition, no normative consumption left. DEPENDENCIES: each amended contract's Dependencies line equals its configured list, sorted and duplicate-free; an independent DFS over the configured graph finds no cycle; closures are T073 43, T083 47, T084 42, T101 42 tasks, each containing the successors it names; T101's new direct T069 edge is not redundant and T068 is reached transitively through T069 and T105 (both declare T068); after the change T063's only consumers are its successor T105 and the APPROVED T064 (retirement route T106), and T066's only consumer is its successor T107, exactly what T105.md:54-59 and T107.md:45-48 require of later CONTRACT amendments; no superseded_by is set anywhere and nothing is retired. IMPACTS INDEPENDENTLY CHECKED: recomputing the register from todo/amendments/*/impacts.json with the workflow's own rules gives 24 open impacts at the freeze base, of which exactly the six resolved here touch the four targets (T073 one, T083 one, T084 two, T101 two), and 18 open impacts in the candidate tree with those four tasks carrying none; the 18 named-but-left-open ids match my recomputation exactly; each of the six is genuine and narrow, matching its recorded required_disposition (A0014:T073 registry-bound evidence and candidate identity with fail-closed legacy/mismatch/downgrade cases and the pool/token/paper order unchanged; A0014:T083 current registry-bound lock with immutability of old locks and old-format/mismatch/unavailable-revision refusals; A0014:T084 registered identity plus schema/registry revision, current-and-legacy rendering, mismatch and no-free-text-substitution; A0013:T084 and A0013:T101 legacy scope findings reconciled against ARCHITECTURE section 2.2 and the T069 run lifecycle; A0014:T101 panel bound to the declared T069 run's validated T105 manifest set with source checksums, dataset/run/member identity, registry revision, deduplication and refusal of incompatible, failed, cancelled and legacy input). AUTHORITY AND HISTORY: one authoritative path is preserved - T073 displays registry-bound artifacts without writing them, T083 reports the lock rather than producing a second one, T084 stays a read model and T101 consumes the run's manifests without acquiring run or lock ownership; immutable history is untouched (T063/T064/T065/T066/T067/T105/T107/T087/T088 and every other contract byte-identical, approvals and evidence fields unchanged) and the old implementation's disposition remains where T105.md:40-49 and T107.md:33-44 already place it (adapt or replace on cutover, versioned legacy readers only, byte-for-byte read-only retention, fail closed rather than fall back). No requirement outside the recorded direction was introduced: every added sentence traces to a line of the Owner direction or of the corresponding A0014/A0013 required_disposition, and all pre-existing acceptance and Must-not clauses are preserved and only extended. VERIFICATION REPRODUCED read-only in this worktree: python -m tools.workflow validate = status OK; tools.check_acceptance check, tools.check_citations check and tools.check_imports check --suppressions docs/implement/ci/suppressions.toml --config todo/config.yaml --architecture docs/spec/architecture/ARCHITECTURE.md report no findings; PYTHONPATH=src pytest tests/test_workflow.py tests/test_workflow_contracts.py tests/test_documentation_links.py tests/test_documentation_citations.py tests/test_acceptance.py = 101 passed; git diff --check clean; the five source/test checksum claims in the record (src/robinhood_lp/reports/manifest.py:118 VALID_STRATEGY_KINDS with five names, the rerun-manifest CLI in src/robinhood_lp/__main__.py) are accurate. NON-BLOCKING OBSERVATIONS: the record's sentence 'A0014 added only the T105-T108 successor rows and the transition paragraph at :171' is an incomplete enumeration - A0014 also annotated the T063-T066 predecessor rows, added the T067 row at :123 and the layer paragraph at :179-183 - so the word 'only' is falsifiable; the operative attribution (A0012 recorded the T087/T088 rows, A0014 did not, and A0014 never touched WEB_CONSOLE.md) is verified correct and no contract text depends on the enumeration, so the verdict is unaffected; the same wording came from review-001's own required-change text. The record's phrases 'this attempt's diff against the attempt base c19ba06 contains exactly one file' and 'the change set against the freeze base is exactly the four contracts and the four depends_on lists' describe the planner-authored change set and omit the controller-written amendment records, which the same record acknowledges elsewhere in the same field. All three review-001 required changes are therefore resolved and nothing else moved, so the candidate may be approved.

## Required changes

- None.

## Unknowns

- None.
