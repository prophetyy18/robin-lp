# A0016 owner amendment review

- Base commit: `b2ee26aa32fdf4403efdccbe358732fb8659d9dc`
- Candidate commit: `069da145d57d17b2678f056fb15bc7111ced60b2`
- Verdict: **FAIL**

## Summary

Independent Owner-amendment review of A0016 (layer CONTRACT; target tasks T073, T083, T084, T101). Identity: candidate 069da145d57d17b2678f056fb15bc7111ced60b2 is a single commit whose only parent is the stated base b2ee26aa32fdf4403efdccbe358732fb8659d9dc (git rev-list --count b2ee26a..069da14 = 1; git log -1 %P = b2ee26a), the detached review worktree is clean at exactly that SHA, git diff --check is clean, the committed todo/amendments/A0016/request.json is byte-identical (cmp) to the runtime record /home/lpdev/lp/.git/robinhood-lp-workflow/A0016-request.json, and the runtime A0016.json records AWAITING_REVIEW attempt 1, layer CONTRACT, task_ids [T073, T083, T084, T101], base b2ee26a and candidate 069da14. DIRECTION AND BOUNDARY: the recorded direction names exactly those four tasks and every directed item is implemented; a field-for-field comparison of base and candidate todo/config.yaml shows the only changed task fields are the four depends_on lists (T073 [T025,T027,T105,T107]; T083 [T082,T107]; T084 [T025,T072,T080,T105]; T101 [T050,T069,T100,T104,T105]) and nothing else - no status, attempt, approved_commit, evidence, replaces, superseded_by, task set, active_task, intent_revision or spec_revision moved; all four targets were PLANNED at attempt 0 at the base and remain so; git diff --name-status reports exactly eight paths - the four target contracts, todo/config.yaml and the three controller-written todo/amendments/A0016 records - and nothing under src/, tests/, tools/, docs/, .github/, .claude/ or todo/schemas/ changed, so there is no implementation, approval or workflow-state change. DEPENDENCIES: each amended contract's Dependencies line equals its configured depends_on list, sorted and duplicate-free; an independent DFS over the configured graph finds no cycle; every target's closure contains the successor(s) it now names (T073 43 transitive tasks including T105/T107, T083 47 including T107, T084 42 including T105, T101 42 including T069/T068/T105); after the change T063's only consumers are the successor T105 and APPROVED T064, and T066's only consumer is the successor T107, exactly what T105's and T107's own downstream requirements demanded (T105.md lines 54-59 'T069, T073, T084 and T101 must consume T105'; T107.md lines 45-48 'replace T066 with T107 in T073 and T083'); no superseded_by is set anywhere, so nothing is retired by this CONTRACT act. IMPACT IDS INDEPENDENTLY CHECKED: recomputing the register from todo/amendments/*/impacts.json at the base shows 24 open impacts, of which each target's open set was exactly the six resolved here (T073: A0014:T073:use-current-evidence-and-candidate; T083: A0014:T083:consume-registry-bound-candidate-lock; T084: A0013:T084:legacy and A0014:T084:present-current-manifest-provenance; T101: A0013:T101:legacy and A0014:T101:assemble-panel-from-current-run-manifests), and after the candidate those four tasks have no open impacts while exactly the 18 other impacts named in the rationale remain open - the resolution is narrow and exact, not broad. Each of the six is genuinely closed by the delivered text. T073 replaces T063/T066 with T105/T107 in Dependencies, Deliverables, Acceptance, Must not and References, shows only current registry-bound evidence and candidate identity, renders legacy artifacts historical and non-current, fails closed on absent, mismatched or downgraded artifacts, and preserves the three journeys, the WEB-GLOBAL-003 write path, the pool/token/paper authorization order, the P07 entry-gate clause and the bounded-scope clause, matching A0014:T073's recorded disposition. T083 replaces T066 with T107, resolves the released candidate only to the current registry-bound lock, keeps T066 locks immutable, byte-identical and unpromotable, and adds current-lock, old-format, mismatch and unavailable-revision verification, matching A0014:T083's disposition. T084 replaces T063 with T105 and requires the recorded registered identity and schema/registry revision with current-and-legacy rendering, mismatch fail-closed and no free-text substitution; its P08 console-scope paragraph now names the T087 (WEB-PAGE-012) and T088 (WEB-PAGE-013) rows and places them explicitly outside this task's acceptance without claiming them, which is what ARCHITECTURE.md section 2.2 (lines 131, 160-161) and WEB_CONSOLE.md (lines 179, 188, 191, 202, and 'does not change the existing acceptance scope of T084-T086' at 188-189/202-203) record, so A0013:T084:legacy is genuinely closed. T101 replaces T063 with T105, adds the direct T069 edge, binds the panel to the declared run's validated manifest set with run/dataset/member/registry-revision/checksum provenance, deduplication and refusal of incompatible, failed, cancelled and legacy input, consistent with T069's lifecycle contract and T063's frozen panel sentence, so A0013:T101:legacy and A0014:T101:assemble-panel-from-current-run-manifests are genuinely closed. One authoritative path is preserved (T073 displays, T083 reports, T084 reads, T101 consumes; no second writer appears), immutable history is untouched (T063/T064/T066/T067/T087/T088 contracts byte-identical; their open impacts stay open), and the old implementation's disposition is already specified in T105's and T107's Replacement and migration sections (adapt or replace, legacy reader only, retain byte-for-byte, never fall back) rather than re-decided here. affected_existing_tasks [] is a defensible claim: T105.md and T107.md already require exactly these re-pointings, T087/T088 keep their own open A0014 impacts, and T085/T086/T090/T093 name only dependencies in their unchanged contract text. Verification re-run read-only in this worktree: python -m tools.workflow validate = OK; tools.check_acceptance check, tools.check_citations check and tools.check_imports check report no findings; pytest tests/test_workflow.py tests/test_workflow_contracts.py tests/test_documentation_links.py tests/test_documentation_citations.py tests/test_acceptance.py = 101 passed; git diff --check clean. Verdict FAIL for record and contract accuracy defects this amendment introduces, all actionable in a retry and all contradicting the committed history: the first required change below shows that todo/phases/P08-operations-and-web/T084.md now attributes the two further P08 web-module rows in ARCHITECTURE.md section 2.2 to A0014, while git blame and git log -S show they were recorded by A0012 (candidate commit 2b64df7) and A0014 added only the T105-T108 successor rows and the transition paragraph; the second and third show the same misreading and other stale citations committed into todo/amendments/A0016/planner-001.json. Non-blocking observations: the planner's narrative that a first T084 draft naming two unresolvable p08 web module paths was reworded cannot be reproduced from the candidate (no draft text is retained), though the final text passes the citation checker; and the rationale's count sentence 'three in T073, three in T083 and four in T084' undercounts T073's legacy-only occurrences even though its substantive claim (all remaining occurrences are legacy renderings or prohibitions, no normative consumption) is verified true.

## Required changes

- Correct todo/phases/P08-operations-and-web/T084.md line 13: the inserted clause 'and - since A0014 - records two further P08 web-module rows outside that triple' misstates the specification history. ARCHITECTURE.md section 2.2's two further P08 web-module rows - T087 `robinhood_lp.web.runs` (line 160) and T088 `robinhood_lp.web.backtest_result` (line 161) - were recorded by A0012 (candidate commit 2b64df7, measured with git blame and git log -S on docs/spec/architecture/ARCHITECTURE.md); A0014's ARCHITECTURE.md change added only the T105-T108 successor rows and the transition paragraph, and A0014 did not touch docs/spec/product/WEB_CONSOLE.md at all. Reword the clause so the paragraph describes section 2.2 as it now stands, which is what the recorded Owner direction required, without attributing the two rows to A0014 - drop the attribution or name A0012.
- Correct the line citations in todo/amendments/A0016/planner-001.json impact_assessment.specification so the committed record matches the candidate tree: the T105/T107 successor rows are at docs/spec/architecture/ARCHITECTURE.md:155 and :157 (not :133/:135), the T073 row is at :159 (not :137), and the T087/T088 rows are at :160/:161 (not :155/:156, which were their A0013-era positions before A0014 inserted the four successor rows). The section's other citations check out (:131 for T084-T086, :142 for T101, WEB_CONSOLE.md :179/:188/:191/:202, PROJECT_GOALS.md :506/:508/:536).
- Correct or drop the occurrence count in todo/amendments/A0016/planner-001.json rationale - 'three in T073, three in T083 and four in T084' undercounts T073, which carries three T063 and three T066 occurrences (six tokens, measured with grep -o), while T083 carries three T066 and T084 four T063. The substantive claim that sentence makes - every remaining T063/T066 occurrence is a legacy-only rendering or prohibition and no normative consumption remains - is verified true, so only the arithmetic needs repair.

## Unknowns

- None.
