# A0010 owner amendment review

- Base commit: `9c90b3522f4a39a122b36984fc1da04350dabfbd`
- Candidate commit: `ea85cd6cd471fab527390e4b9bed6ec421111534`
- Verdict: **PASS**

## Summary

Independent Owner-amendment review of A0010 (SPEC layer; targets T064, T065, T101, T102). Verdict: PASS. Identity and scope: candidate ea85cd6 is the single child of base 9c90b35 (parent recorded in the commit object), the detached review worktree is clean at exactly that SHA, and git diff --check is clean. The changed path set is exactly docs/spec/strategy/STRATEGY_ECONOMICS.md, docs/spec/research/DATASET_AND_EVALUATION.md, todo/phases/P06-backtesting-and-strategy/T064.md and T065.md, plus the two amendment records todo/amendments/A0010/request.json and planner-001.json that finish-amendment writes after its own path-scope check (the same pattern earlier amendment reviews accepted). Every path lies inside the SPEC layer's permitted surface (the named task contracts plus docs/spec/); no src/, tests/, tools/, .claude/, todo/schemas/, docs/intent/, docs/implement/, evidence or review path changed. Recorded direction: todo/amendments/A0010/request.json is byte-identical to the runtime record /home/lpdev/lp/.git/robinhood-lp-workflow/A0010-request.json (diff empty); the recorded target set T064, T065, T101, T102, the layer SPEC and target_status PLANNED match the runtime A0010.json (AWAITING_REVIEW, attempt 1, base and candidate identical to this review) and the Manager prompt; all four targets are PLANNED at attempt 0 in todo/config.yaml. todo/config.yaml is byte-identical to base, so no status, attempt, commit SHA, evidence pointer, review record, approval field or dependency entry moved, no task was activated and no attempt was consumed. Item-by-item verification: (1) STRATEGY_ECONOMICS section 7 - at base the removed names AdmissionEligibilitySnapshot, MarketFeatureSnapshot, CandidatePolicy and EconomicEvaluator occurred nowhere except that one list (repository-wide git grep at base), while the surviving names are the implemented, contract-specified vocabulary: AdmissionSnapshot/MarketSnapshot/PortfolioSnapshot (base.py:557/636/727; T060's unchanged deliverable of immutable versioned admission/market/portfolio snapshots), RegimeModel/FeeOpportunityModel (base.py:831/862), RegimeAssessment/FeeOpportunityAssessment/CandidateAction (base.py:894/1017/1152); StrategyDecision is only the engine callback's return type (engine.py:198, enforced at engine.py:610-615), so moving it out of the component list to the boundary paragraph satisfies the direction, and the component-vs-output naming rule is kept and attributed to T060 (T060 lines 12-17 state the same rule). The new paragraph matches the implementation: StrategyDecisionRequest (engine.py:175) carries pool_key_id, chain_id, decision_time, visible_events and ledger; strategy_callback is engine.py:436; the engine imports no strategy snapshot or component module. Adapter ownership by T065 rests on approved-task evidence: T061's residual risk records that the T060 CandidateAction contract is not yet consumed by the engine and that T062 / T065 will wire the concrete strategy components into the same callback shape (todo/evidence/P06/T061/attempt-001-developer.json:32), and T062's records that none such adapter exists today and the T060 contracts do not provide a StrategyCallback-shaped entry point (todo/evidence/P06/T062/attempt-001-developer.json:36). T060, T061 and T062 are byte-identical to base, as the direction required. T065's added deliverable sentence and References pointer are the matching clause; its Acceptance, Must not and Dependencies sections are byte-identical to base. T101 and T102 are byte-identical to base with the basis stated: T102 says only that the model occupies the replaceable regime and fee-opportunity interfaces and is evaluated through the engine, which the corrected section 7 now names, and T101's Outcome already carries the same ADR-015 per-pool window and the same 0.1008-second, roughly twelve-day, roughly 3.4-thousand-five-minute-bar numbers the corrected DS-030 states, so neither text is made wrong. (2) DS-030 - the retired ten-million-block window and its derived 11.7-day figure are gone; ADR-015 clause 5 forbids citing it as the research range and supplies every replacement number (0.1008-second measured interval, pinned reference pool initialised about 12 days before the last probe, roughly 3.4 thousand five-minute or 288 hourly bars, about 65 million blocks or roughly 76 days of history), and the roughly 22-thousand-five-minute-bar cap follows arithmetically from that 76-day figure; the clause's substance (deliberately small model families because measured sample size is the binding constraint, panel dominated by a few shared market regimes, deep learning out of V1) is retained. (3) ECO-REGIME-001 - the added metric matches CTRL-MARKET-001 in docs/spec/operations/OPERATOR_CONTROL.md (return_5m = close_usdg / open_usdg - 1 at the end of a complete period, unfinished period must not trigger) and G-STRATEGY-SPIKE-01's complete-5-minute, above-100-percent wording; the strict-greater trigger matches the implementation (FIVE_MINUTE_UP_SPIKE_FRACTION is 1<<64 = 100 percent at quote.py:104 and UP_SPIKE fires only when post exceeds pre and the move strictly exceeds the threshold at quote.py:1548; both rules share FIVE_MINUTE_RULE_WINDOW_SECONDS = 300 at quote.py:97 and the same T053 point-in-time USDG object, which T053's acceptance already requires for both 5-minute extreme-move rules); the alignment and clock sentence matches the bar layer (FeatureBar carries data_time and availability_time; window ends must be a positive multiple of the window size at bars.py:1617 and 1643; ARCHITECTURE.md's determinism policy forbids wall-clock reads in deterministic paths), and the rule remains strictly in-strategy and non-global with the minus-80-percent AUTO_EXIT ownership unchanged. (4) DS-035 - the halt-or-degrade obligation now has exactly one home, the new clause DS-035 in DATASET_AND_EVALUATION.md section 5, and T064 is reduced to references in Deliverables, Acceptance and References, so the two documents no longer both define it; that is the choice the direction allowed, and it is the deferral the A0009 review recorded. DS-035's content matches the substance the contract carried (one declared outcome per entry, declared before the run, a named-reason-code halt or an entry-named deterministic fallback, never inferred or improvised afterwards; degradation is not undefined behaviour and not a forced trade, citing DS-041), and its generality is what the direction asked for. T064's catalogue deliverable and every acceptance clause are retained with the citation swap; no Must not clause changed in either contract. Preservation and boundaries: a programmatic section diff shows T064 and T065 keep byte-identical headers, Dependencies and Must not sections, and T065's Acceptance is byte-identical; T064's Acceptance differs only by adding the DS-035 citation while keeping every clause and DS-041. No dependency edge was added or removed, and the contract Dependencies still match todo/config.yaml depends_on (tests/test_workflow_contracts.py::test_every_task_is_a_separate_complete_contract passes). No acceptance criterion, safety, risk, promotion or precision rule was removed or relaxed, and the only specification requirement replaced is the retired ten-million-block basis the direction ordered replaced; no unrelated requirement was introduced, and no URL, credential, key material or environment value appears in the added lines. Evidence run in this worktree: python -m tools.workflow validate returns status OK; tests/test_documentation_links.py and tests/test_workflow_contracts.py 6/6 passed; tests/test_workflow.py 44/44 passed; tests/test_no_signing_paths.py, tests/test_smoke.py and tests/test_config.py 68/68 passed when src is placed on PYTHONPATH (the package is not installed into this detached worktree, which is unrelated to the amendment). Non-blocking observations, outside this amendment's four items and not required changes: (a) T102's Dependencies block lists T061 and T101 only, while its References compare the model against T065's rule implementation and its walk-forward evaluation now runs behind the T065-owned adapter; that is a pre-existing plan-structure question, T102 is byte-identical to base, and it cannot bite because a task may be readied only while no previous task is unfinished, so T065 is approved before T102; the planner's rationale calls a dependency edit something the SPEC-layer direction does not permit, which is imprecise (the layer does permit a named task's dependency fields), but the outcome of making no dependency change is within the direction's adjust-only-where-wrong rule. (b) The rationale attributes the no-wall-clock rule to ADR-006 plus ARCHITECTURE.md section 4; ADR-006 is dependency direction and the wall-clock rule sits in ARCHITECTURE.md section 3, but no delivered text rests on that citation.

## Required changes

- None.

## Unknowns

- None.
