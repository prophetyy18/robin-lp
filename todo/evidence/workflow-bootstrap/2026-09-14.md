# Workflow Bootstrap verification — 2026-09-14

This record covers workflow infrastructure only. It does not approve T001 or any
business implementation task.

## Runtime

- Project Python: 3.12.14 from `/home/lpdev/miniconda3/envs/robinhood-lp`.
- Claude Code: 2.1.269.
- Provider: MiniMax Anthropic-compatible API, China endpoint.
- Required model: `MiniMax-M3[1m]`; fallback disabled.
- Secrets: user-level Claude Code settings only; no key value was copied here.

## Provider verification

On 2026-09-14 a minimal direct Anthropic-compatible request returned HTTP 200.
The request used `MiniMax-M3[1m]`; the response identified `MiniMax-M3`.

A separate headless Claude Code invocation completed with `is_error: false`,
returned `OK`, and reported a 1,000,000-token context window. Claude Code also
printed its local `claude-code:unrecognized_model` diagnostic for the third-party
identifier before the successful JSON result. The workflow must therefore parse
the final result envelope and must not treat that diagnostic alone as failure.

## Mechanical exercise

`tests/test_workflow.py::test_fake_fail_repair_pass_workflow` uses a deterministic
fake Claude-compatible process while exercising real temporary Git repositories,
branches, worktrees and commits. It proves the controller's mechanical sequence:

```text
READY -> candidate -> FAIL -> CHANGES_REQUESTED
      -> fresh developer -> candidate -> PASS -> APPROVED -> fast-forward
```

The deterministic fake is intentionally not evidence of model quality. The live
smoke above is intentionally not evidence that any task acceptance contract has
been satisfied.

## Quality gates

Executed from the repository root with the project Python after the M3 runtime
constraint was added:

- `python -m tools.workflow validate`: `status: OK`.
- focused workflow suite: 10 passed.
- full pytest suite: 275 passed, 2 skipped.
- `ruff format --check .`: 142 files already formatted.
- `ruff check .`: all checks passed.
- `mypy src tests tools/workflow`: no issues in 42 source files.

The two skips are pre-existing and remain explicit gaps: an ABI artifact SHA-256
is still a manual annotation, and one reordered-currency vector is covered by a
separate Python invariant test. They are not counted as passing evidence and do
not approve T001/T013/T021.
