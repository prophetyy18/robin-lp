# Owner-authorized bootstrap — split active T109

This record documents a one-time bootstrap act directed by the Owner on 2026-09-21. It changes
plan structure and the frozen contract of an already active task because the ordinary amendment
lane refuses to start while a product task is unfinished and the active attempt had consumed the
workflow's only continuation. It is not approval evidence for T109, T110 or T111.

## Trigger and preserved state

- Effective state before bootstrap: T109 `IN_DEVELOPMENT`, attempt 2, base commit
  `0ef2ce08dbd7bbbf727b892ef2cfa16a41269ebf`, retained branch
  `workflow/t109-attempt-001`, continuation count 1.
- Attempt 1 candidate `d0d282f42ea10eab70cbfebc1f606e697ace0e9c` and independent FAIL review
  `todo/reviews/P06/T109/review-001.{json,md}` remain immutable.
- The attempt-2 worktree and all uncommitted implementation changes are preserved. No acceptance
  item is declared complete by this bootstrap and no candidate or approval SHA is manufactured.

## Split

- T109 retains causal T061 scheduling/cursor binding, immutable simulation evidence, the current
  dataset-reference manifest/report authority, atomic publication, T069/T105 lifecycle migration,
  legacy read-only behavior and predecessor-success-path removal.
- T110 owns qualified `MarketState`, `RunState` and `ReplayFrame`, heterogeneous reconstruction and
  T061 golden equivalence, sparse checkpoint replay, no-callback proof and T104-only fee projection.
- T111 owns the validated T101/T102/T106 compatibility adapter, approved-consumer integration and
  current-path removal of T069/T105 publication.

The three contracts jointly retain every Outcome, Deliverable, Acceptance, Must-not and Reference
obligation from the pre-bootstrap T109 contract. Product Intent and Spec semantics are unchanged.
T087, T088 and T103 now depend on T110 because they consume replay state. T096 depends on T111 so
its final dossier closes the entire chain. Manifest/evidence-only consumers continue to depend on
T109.

## Continuation recovery

After the split commit, bootstrap refreshes only T109's protected-file snapshot to the exact
post-split hashes and resets the derived runtime continuation counter from 1 to 0. The next normal
`continue-develop T109 --max-turns-exhausted` call increments it to 1 and produces a fresh visible
Developer prompt. This is a one-time recovery for the narrowed contract, not a repository-wide
increase in continuation allowance. The tracked T109 attempt/status/review/candidate history is not
rewound.

## Required verification

- `python -m tools.workflow validate`
- `python -m tools.check_citations`
- `python -m tools.check_acceptance`
- `python -m tools.check_imports`
- workflow tests covering configuration and plan invariants
- `git diff --check`

The continued Developer must still satisfy the narrowed T109 contract and produce a normal
`CANDIDATE_READY` handoff. An independent Reviewer must review the exact new candidate before any
approval. T110 and T111 remain `PLANNED` and each requires its own independent develop/review cycle.
