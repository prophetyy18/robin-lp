# T015 — Make test_repository_workflow_configuration_is_valid scale with the task catalog

> Phase: `P00`
> Contract status: `FROZEN_FROM_BASELINE_6C31778`

## Dependencies

T014

## Implementation status

`tests/test_workflow_contracts.py:27` hard-codes
`assert len(config["tasks"]) == 53`. After T014 was registered, the
config now carries 54 (then 55 with T015 itself) task entries, so the
assertion fails and the global pytest baseline reports
`1 failed / 372 passed / 6 skipped`. The 1 failure is not a
spec/protocol defect; it is a brittle catalog-count assertion that does
not match the workflow's intent of "every task is a separate complete
contract".

## Outcome

`tests/test_workflow_contracts.py::test_repository_workflow_configuration_is_valid`
asserts a lower bound on the task count that holds across any future
catalog growth (T015 included) while still encoding the historical
"at least T000–T004 were seeded in the baseline" invariant. No other
test, fixture, config, or contract file is modified.

## Deliverables

1. `tests/test_workflow_contracts.py` line 27: replace
   `assert len(config["tasks"]) == 53` with
   `assert len(config["tasks"]) >= 53`. No other line in the file is
   changed.
2. The three remaining assertions in the same test (`T000["status"] ==
   "APPROVED"`, `T004["status"] == "APPROVED"`, the
   `{"T000","T004"} <= {task_id for ... APPROVED}` subset check) stay
   verbatim.

## Acceptance

- `pytest tests/test_workflow_contracts.py -v` reports all four tests
  in that file as PASS, including
  `test_repository_workflow_configuration_is_valid`.
- `pytest -q` (full suite) reports `0 failed / N passed / 6 skipped`
  where N is at least the previous baseline count (372) plus any
  tests added by future tasks; the T014-induced `1 failed` regression
  is gone.
- `ruff check` exit 0; `ruff format --check` exit 0; `mypy src tests`
  exit 0; each gate run twice with byte-identical normalised output.
- `git diff --stat` of the candidate shows only
  `tests/test_workflow_contracts.py` modified, exactly `+1/-1`.
- `git diff --` is empty for `todo/config.yaml`, `todo/phases/`,
  `docs/`, `tools/`, `tools/workflow/`, and any other path.

## Must not

- Modify any file other than `tests/test_workflow_contracts.py`.
- Re-introduce a hard-coded task count (e.g. `== 54`) — the
  lower-bound form is the contract.
- Touch `todo/config.yaml`, `tools/workflow/`, or any contract
  markdown outside T015's own delivery.
- Drop or weaken the `T000` / `T004` APPROVED seed assertions or the
  subset check in the same test.
- Add or remove any `test_*` function in the file.

## References

AGENTS.md §7 (minimum diff), WORKFLOW.md §"Source of truth" (config.yaml
is the sole machine-readable source of task state). No R3 / R8 / R22
references — this is a Python test-only fix.
