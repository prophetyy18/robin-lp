# T041 — Reconstruct tick-liquidity state

> Phase: `P04`
> Contract status: `FROZEN_FROM_BASELINE_6C31778`

> Owner amendment A0002 (2026-09-18): tick and liquidity reconstruction is per pool
> over that pool's own qualified window.
>
> Owner edit 2026-09-19 (ADR-015): the window is now the pool's own `Initialize`
> block through the agreed finalized block, so `pool_init_outside_window` is no
> longer a reachable outcome. This note supersedes that exclusion; it adds no
> deliverable to this contract, and the approved implementation is unchanged.

## Dependencies

T040, T037, T038

## Input scope

Tick and liquidity reconstruction is per pool, over that pool's own qualified
window and data root. Reconstruction state is never merged, shared or
carried across pools, and a pool with no qualified dataset is not reconstructed.

A pool whose `Initialize` block cannot be located is a failed resolution that stops
acquisition upstream: this task must not reconstruct it from a partial window, from a
later-state bootstrap, from a neighbouring pool's data, or by widening the window.

## Outcome

initialized ticks and active-liquidity crossings obey V4 invariants.

## Deliverables

gross/net tick updates, bitmap/initialized state, crossing logic,
position key treatment, and invariant checks.

## Acceptance

add/remove/poke, boundary crossing both directions, same-block multi-
action, max liquidity, negative gross, spacing and overflow vectors match core.

## Must not

derive owner inventory from pool liquidity events or assume one event
equals one user position lifecycle.

## References

T037, T038, T040, ADR-010, ADR-012, ADR-013, ADR-015,
`docs/spec/protocol/PROVIDER_FACTS.md`, R3, R4.
