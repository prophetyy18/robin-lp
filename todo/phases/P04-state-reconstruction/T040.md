# T040 — Build deterministic event replay

> Phase: `P04`
> Contract status: `FROZEN_FROM_BASELINE_6C31778`

> Owner amendment (2026-09-16): consume P03 `ProtocolFeeUpdated` history so
> replay distinguishes the emitted combined swap fee from LP-owned fees.
> Owner amendment A0002 (2026-09-18): replay consumes the per-pool datasets T038
> qualifies, one replay input per data root; the superseded 2026-09-18 reference
> dataset is not a replay input.

## Dependencies

T012, T030, T034, T037, T038

## Input datasets

Replay consumes the dataset that T038 qualifies for each pool, one replay input
per pool, addressed by that pool's own data root and spanning that pool's pinned
finalized window. A run states which pool and which data root it replayed; one
replay input must not mix two pools' events, and no pool's replay may consume
another pool's data root.

The superseded 2026-09-18 reference dataset
(`run-680e65f4a59842d98b1712a45280779d`) is **not** a replay input and is not a
satisfied entry condition: its `event_index` and its Parquet partitions disagree,
so its own baseline comparison fails closed (recorded in
`docs/spec/protocol/PROVIDER_FACTS.md`). Replay must not read, repair, re-qualify,
overwrite or merge it, no checkpoint may be derived from it, and replay must not
require or assume the superseded 1,000,001-block reference range.

## Outcome

chunking/storage order cannot change observable pool state.

## Deliverables

total ordering by block number, transaction index, log index;
replayed price/tick/active liquidity/volume/effective combined swap fee,
directional packed protocol-fee state, and checkpoint state. The replay output
must preserve enough exact integer evidence for T051 to separate protocol fees
from LP fee growth using pinned V4 rounding semantics.

## Acceptance

shuffled/chunked/restarted inputs yield identical checkpoints; duplicate,
missing transaction index, impossible transition, and unknown pool fail explicitly.
For dynamic-fee pools, preserve the effective fee emitted by each `Swap`; do not
claim event-only reconstruction of unobserved between-swap fee updates. Protocol
fee changes in both directions, same-block update-before-swap ordering, and the
zero-protocol-fee initialization state match pinned core vectors.

## Must not

use ingestion order, wall time, future calls, or token display decimals
in protocol state.

## References

T037, T038, ADR-010, ADR-012, ADR-013,
`docs/spec/protocol/PROVIDER_FACTS.md`, R4, R14.
