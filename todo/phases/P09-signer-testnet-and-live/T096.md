# T096 — Produce final V1 acceptance and traceability dossier

> Phase: `P09`
> Contract status: `FROZEN_FROM_BASELINE_6C31778`

## Dependencies

T073, T087, T088, T095, T097, T108, T111

## Outcome

the owner can determine whether every V1 goal is met from linked evidence.

## Deliverables

goal → spec → task → test/evidence matrix; clean install/deploy/operate/
backup/restore runbook; supported identities/limits; research, paper, testnet, security and
mainnet manifests; known limitations and incident history.

The matrix covers the plan this gate now governs and names each delivery with the goals it
carries instead of relying on an unnamed transitive path. T112 is the current registry-bound
product-run and manifest/report/evidence authority; T110 is the qualified exact historical replay
authority; and T111 is the approved-consumer cutover behind `G-STRATEGY-REGISTRY-01`,
`G-BACKTEST-RUN-01`, `G-HISTORICAL-REPLAY-01` and `G-RUN-REPLAY-01`. Their evidence maps
`DS-005`–`DS-007`, T104's sole fee-projection/provenance boundary and threat `T-22`. T069 and
T105 are named only as predecessor delivery evidence whose lifecycle, identity, checksum,
compatibility and migration guarantees the T109→T110→T111 chain preserves; they are not current
artifact authority. T109 is preserved as a historical read-only predecessor manifest and
evidence pending the SUPERSEDE amendment recorded by A0040, not as the current evidence source
this gate reads from.

The other registry-bound deliveries are named with their current goals and specification clauses:
T106 (the schema-bound robustness surfaces and reports that `DS-021`,
`DS-022`, `DS-035` and `DS-041` require), T107 (the registry-bound candidate lock and search
surface, and the frozen identity, schema and registry revision, behind
`G-STRATEGY-REGISTRY-01`, with `docs/spec/strategy/STRATEGY_ECONOMICS.md` section 6 and
`DS-031`), and T108 (the current authoring guide, worked example and drift tests that keep
`G-STRATEGY-REGISTRY-01` teachable, with `docs/spec/strategy/STRATEGY_ECONOMICS.md` section
7). The A0012-era deliveries whose goals this gate already claims to check are named the same
way: T068 (the strategy registry behind `G-STRATEGY-REGISTRY-01`), T073 (the pre-paper
authorization entry behind `G-PAPER-AUTHORIZE-01` and `G-PERM-WRITE-01`, with
`WEB-GLOBAL-006`), T087 (the registry and run page whose coverage provenance `WEB-PAGE-012`
requires), T088 (the backtest result page whose layering, attribution residual and coverage
rendering `G-BACKTEST-RESULT-01`, `WEB-PAGE-013` and `WEB-GLOBAL-007` require), and T097
(the read-only and reduce-only CLI control surface behind `G-PERM-WRITE-01`, `G-EXIT-01` and
`G-CONTROL-01`, with `CTRL-CLI-001`). Each of T112, T110, T111, T106, T107 and T108 is mapped to its own
independently reviewed evidence rather than to the predecessor's approval, review record or
artifact, and every other `G-*`, `ADM-*`, `WEB-*`, `ECO-*` and `CTRL-*` obligation maps to
its delivering task and to that task's own independently reviewed evidence the same way. T109
remains readable only as a historical read-only predecessor manifest and evidence, not as a
current evidence source for any obligation in this gate.

The gate's dependency closure contains each named delivery: T073, T087, T088, T097, T108 and
T111 by direct dependency; T112 through T073, T087, T088 and T108 (and through T110, reached
from T111), as the current product-run and paired simulation-evidence authority; T110 and T109
through T111, with T109 the historical read-only predecessor manifest and evidence pending the
SUPERSEDE amendment recorded by A0040; T068 through T087, T088 and T108; T069 and T105 solely
through T109's explicit replacement dependencies; T107 through T073; and T106 through T107 and
T111. No named current delivery is reached only by an unnamed path, and the closure is
recomputed rather than assumed whenever the graph changes.

Each claim resolves to the current artifact of the delivery it names — T112's manifest and paired
simulation-evidence, T110's qualified replay frames, T111's consumer adapter,
the schema-bound robustness surface and report, the registry-bound
candidate lock, and the current guide, example and drift tests — and never to a predecessor
artifact. Predecessor records (a T069 run, T105/T063 manifest, T109 manifest and paired
simulation-evidence, `T064` surface or report, `T066` lock, or `T067` guide) appear only as
linked, read-only history: they are not rewritten, re-derived or backfilled, and no goal is
marked met on their authority. A goal whose required delivery
is absent, failed, cancelled, legacy-only or not independently approved stays unmet with the
reason recorded; no plan-level index entry, task checkbox or predecessor approval stands in
for the delivery's own reviewed evidence.

T096 keeps two authority distinctions the registry and replay work do not change: T108's registration
guidance and worked example are documentation and grant no permission to register,
parameterise, publish, run, trade or promote; and no artifact of T112, T110, T111, T106 or T107 —
manifest, paired simulation-evidence, robustness report or candidate lock — carries execution, approval, paper or live
authority, so none of them can satisfy T094 by itself. T112 manifest and paired
simulation-evidence, T110's `MarketState`, `RunState` and `ReplayFrame`, and T111's adapter reconstruct or expose history and confer no such
authority.

## Acceptance

every `G-*`, `ADM-*`, `WEB-*`, `ECO-*` and `CTRL-*` maps to passing evidence or
an explicit blocker; clean-room reproduction succeeds; all required blockers are closed; a
test asserts that the configured dependency closure of T096 contains T068, T069, T073, T087,
T088, T097, T105, T106, T107, T108, T109, T110, T111 and T112, distinguishes T112 as current from T069/T105/T109 as
predecessors, and maps both replay goals, `DS-005`–`DS-007`, T104 and `T-22` to their evidence
(test); each named delivery resolves to its current artifact — T112's manifest and paired
simulation-evidence, T110 replay frames, T111 compatibility adapter, the schema-bound
robustness report, the registry-bound candidate lock, and the
current guide, example and drift tests — and the matrix records that delivery's positive,
negative, compatibility, migration and old-path-unreachable evidence, including the T108
guide, example and drift tests (test); the T112/T110/T111 evidence chain (with the T109→T110→T111 chain preserved as a compatibility and migration guarantee) includes the A/B/C
delayed-fill causality proof, creation-time cursor/ordinal agreement, T104 equivalence/provenance,
consumer compatibility, atomic migration, canonical no-copy storage, no strategy-callback replay,
no post-sort repair and both predecessor-success-path-unreachable proofs (test); a predecessor artifact, a legacy-only delivery, a
delivery that is failed, cancelled or not independently approved, and an old-path artifact
offered as current each leave their goal unmet with the reason recorded instead of passing
(test, fixture); predecessor manifests, surfaces, locks, guides and review records stay
linked and byte-identical, and no matrix entry rewrites or re-derives one (fixture, test);
and absent or unapproved T112, T110 or T111 leaves both replay goals unmet even if T069/T105/T109
evidence exists (test); and T096 claims no permission from T108's guidance and no execution,
approval, paper or live authority from a T112, T110, T111, T106 or T107 artifact (test).

## Must not

use task checkmarks without evidence, omit failed experiments/incidents, or claim
profitability when the acceptance standard is safety/correctness; or count a predecessor
artifact, approval or record as current evidence for any goal; or mark a goal met through a
delivery that is absent, failed, cancelled, legacy-only or not independently approved; or
read T108's registration guidance or example as a permission, or a T109 manifest/evidence, T110
replay frame, T111 adapter, T106 report
or T107 candidate lock as execution, approval, paper or live authority; or rewrite, re-derive
or backfill a predecessor artifact or review record; or let T069/T105 predecessor evidence,
missing causality/cursor/provenance proof, or a task checkbox satisfy the replay goals.

## References

T073, T087, T088, T097, T068, T069, T104, T105, T106, T107, T108, T109, T110, T111, T112,
`docs/implement/TRACEABILITY.md` (the plan-level index this dossier extends into the goal →
spec → test/evidence matrix), `docs/intent/PROJECT_GOALS.md` (`G-STRATEGY-REGISTRY-01`,
`G-BACKTEST-RUN-01`, `G-BACKTEST-RESULT-01`, `G-HISTORICAL-REPLAY-01`, `G-RUN-REPLAY-01`,
`G-PERM-WRITE-01`, `G-PAPER-AUTHORIZE-01`,
`G-STRATEGY-MAINTAIN-01`, `G-LIVE-01`, `G-LIVE-GATE-01`),
`docs/spec/product/WEB_CONSOLE.md` (`WEB-PAGE-012`, `WEB-PAGE-013`, `WEB-GLOBAL-002`,
`WEB-GLOBAL-006`, `WEB-GLOBAL-007`), `docs/spec/operations/OPERATOR_CONTROL.md`
(`CTRL-CLI-001`), `docs/spec/research/DATASET_AND_EVALUATION.md`,
`docs/spec/strategy/STRATEGY_ECONOMICS.md`, and the Phase 9 exit gate this dossier satisfies
in `todo/phases/P09-signer-testnet-and-live/README.md`.

T112 is the current registry-bound source of manifest and paired simulation-evidence this
gate reads from for the final matrix; T109 is preserved only as a historical read-only
predecessor manifest and evidence pending the SUPERSEDE amendment recorded by A0040.
