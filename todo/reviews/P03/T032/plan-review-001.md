# T032 planning review

- Base commit: `d301289583b4f5132357ca13904fe8ca73b6371d`
- Candidate commit: `1f6be8b8d0eadd10c8f8dc1d0fe30ae856700d9c`
- Verdict: **PASS**

## Summary

The candidate merges the frozen baseline and the 2026-09-16 Owner amendment into a single unified T032 contract body. Top `> Contract status: FROZEN_FROM_BASELINE_6C31778` marker and `> Owner amendment (2026-09-16)` block are both removed; only audit-trail mentions of those literal strings remain inside the new `## Binding direction reference` subsection (lines 378-379), which is explicitly labelled as audit trail and not a second source of binding direction. Decision 4 (provider failover, scanned-empty / failed / cancelled boundary, reason codes including `budget_exhausted`, no-local-skip-then-complete hard constraint, remaining-budget bound from operator injection or provider usage API with no assumption of unused monthly quota, provisional future-suffix not promoted across a failed / uncovered gap, `complete=false` on budget exhaustion) is fully folded into Outcome, Deliverables (Scanned-empty / failed / cancelled boundary, Provider routing and failover), Acceptance (failed-intervals-prevent-completion, fixtures covering `budget_exhausted` + `complete=false`, no reclassification of budget-exhausted to empty), Must-not (no-local-skip-then-complete, no provisional promotion past uncovered gap, no budget-exhausted-as-complete, no cancelled/skipped-as-empty), and References (R9, R11, R23, R24, R25, ADR-002, ADR-010, ADR-011). Decision 5 (capability-driven A+B routing, one pool-filtered topic0 OR covering Initialize / ModifyLiquidity / Swap / Donate + inherited ProtocolFeeUpdated with topic1 fixed to poolId, Robinhood public RPC as initial wide-range log source with explicit non-secret application User-Agent and 403 handling, Alchemy Free archive + block-pinned + sampled verification, failover only within measured capability and remaining budget, cold-start scans from selected pool's registered Initialize block to requested end block with shorter backtest start not truncating reconstruction prefix, warm incremental only with previously qualified local checkpoint, one non-hydrated block header per distinct event block, no hydrated blocks / all transactions / all receipts) is fully folded into Outcome (cold-start / warm incremental split, Initialize-to-end-block scan, no-truncation rule, suffix-only warm path, block-header policy), Deliverables (range planner, preflight probe + capability/budget ledger with remaining-budget bound, provider routing and failover, deterministic normalization pipeline reusing T030/T031, block header policy), Acceptance (cold-start vs warm fixtures, kill/restart at each boundary, overlapping/concurrent-run convergence, every-block-covered-exactly-once, run manifest records logical RPC calls / HTTP requests / bytes / rows / provider units / elapsed time / deviations, checkpoint rejection on manifest / block hash / schema / decode / coverage prefix mismatch), and Must-not (no fixed provider-side block cap assumption, credential-bearing URLs reduced to aliases, no hydrated blocks / transactions / receipts in ordinary pool-history ingestion, one combined filter suffices). Path compliance: the Planner's actual content change is limited to todo/phases/P03-ingestion-and-storage/T032.md (the merge). The two auxiliary files in the diff (todo/config.yaml workflow_state and T032.status transitions from PLANNING to AWAITING_PLAN_REVIEW, and the new todo/evidence/P03/T032/attempt-001-planner.json evidence pointer) are controller-owned `finish_plan` wrap-up actions per tools/workflow/core.py lines 2352-2434 and match the established T030/T031 pattern (d565963 for T031, prior T030 candidates); they are not Planner overreach, do not touch Intent / Spec / other task contracts / workflow controller / agent definitions / review records / commit SHAs / model selection / approval data, and preserve the original attempt counter, base_commit / candidate_commit identities, evidence pointer slot, and task dependency graph. The merged contract is consistent with ADR-002 (two-layer storage with raw envelope vs normalized identity / content hash via T030/T031 reuse, atomic durable checkpoint), ADR-010 (capability-driven A+B routing with remaining-budget bound, Robinhood explicit non-secret application User-Agent, Alchemy Free archive role), and ADR-011 (single pool-filtered topic0 OR + topic1 poolId filter). Developer can take this contract and execute attempt 002 directly without further planner input.

## Required changes

- None.

## Unknowns

- None.
