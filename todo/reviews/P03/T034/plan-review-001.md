# T034 planning review

- Base commit: `78492ba0bb927886b468b7c59fd76f29c40ccb74`
- Candidate commit: `ba5d0a1d7c689ea0f6de49b3c301e5ab1ac2ddc8`
- Verdict: **PASS**

## Summary

The candidate merges the T034 frozen baseline and the 2026-09-16 Owner amendment into a single executable contract body, then appends a 'Binding direction reference' subsection as audit trail. The dual-layer top markers (`Contract status: FROZEN_FROM_BASELINE_6C31778` and `Owner amendment (2026-09-16):`) are removed from the contract head and appear only inside the audit trail as text describing what was removed. Outcome, Deliverables, Acceptance, Must-not, and References now form one unified body. Decision 6 is fully absorbed: the five deterministic required-sample rules (per run: chain ID / genesis hash / fixed-block block hash / PoolManager and StateView runtime code hash; per committed block-range partition: opening and trailing 10-block windows; per actually-observed event type: first 10-block window containing the smallest EventKey with duplicate-window dedup; per provider failover: pre/post 10-block windows with overlap-merge allowed and original-endpoint unrestored data staged-but-unqualified; result-bearing requirement when the dataset contains any events, all-empty capability samples non-substitutable) are stated verbatim, sample selection is locked to PoolId / partition bounds / EventKey / manifest with no RNG and no wall clock, and complete=true is gated on every required sample agreeing AND no other gap / error / budget failure. Decision 8 is fully absorbed: the three-value enum (unknown_not_proven / known_correlated / evidenced_independent) is named, the current state UNKNOWN_NOT_PROVEN is recorded explicitly, the cross_endpoint_agreement-only wording rule is stated, the independent_provider_agreement ban is stated, the must-not-be-blank rule is stated, and correlated failure is kept as an explicit residual risk; UNKNOWN_NOT_PROVEN does not block ingestion or backtest research but T034 must surface the limitation. Acceptance retains seeded-defect fixtures with named reason codes; complete=true requires zero unexplained gaps / errors, no exhausted budget, successful required A+B samples, and recorded dataset manifest checksum; empty capability probes count for range acceptance only; cold-start covers from the selected pool's Initialize; warm run verifies qualified checkpoint and complete suffix; backtest-window-only without Initialize or qualified checkpoint is incomplete. Must-not covers warning counts not disappearing in aggregates, third-party match not as proof, no independent_provider_agreement claim, state not blank, state not flipped to evidenced_independent without separate auditable evidence, no RNG or wall clock for sample selection, and no relaxation of any Acceptance clause. References include R12, R13, ADR-002, and ADR-010. ADR-002 and ADR-010 are referenced but not reopened (confirmed in the audit trail). The contract is internally consistent, the merged body is one executable source, and a Developer can implement attempt 002 directly from the contract alone. The config.yaml workflow_state transition PLANNING -> AWAITING_PLAN_REVIEW and the new attempt-001-planner.json evidence file are the controller's standard finish-plan commit payload (tools/workflow/core.py:2397-2417), not unauthorized Planner edits; the Planner Agent's only contract-layer change is the T034.md merge. The base and candidate commit SHAs match exactly.

## Required changes

- None.

## Unknowns

- None.
