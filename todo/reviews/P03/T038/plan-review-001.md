# T038 planning review

- Base commit: `4f93bbc99ce7b712683f1fadcca11954f7a4d6b5`
- Candidate commit: `6cd78fc94aa5eaf75a4f419d5443507594513a28`
- Verdict: **PASS**

## Summary

The planning candidate correctly applies the recorded Owner direction (todo/triage/P03/T038/owner-decision-001.json) to resolve the T038 contract defect reported by the previous TRIAGE_REQUIRED handoff. (1) todo/phases/P03-ingestion-and-storage/T038.md Reference targets section (lines 26-45) re-labels the pinned 20-byte value 0xEd50bDeeA8aDC232f159486192a4157281D722ff on chain id 4663 from 'PoolId' to 'second pool hook contract address' and explicitly states it is a lookup signal equal to the decoded 'hooks' field of the second pool's PoolKey, not a V4 PoolId and not a keccak256 digest. V4 PoolId is correctly defined inline as keccak256(abi.encode(PoolKey)) = 32 bytes. The resolution method is replaced from a PoolId-filtered Initialize scan to an Initialize log scan whose decoded hooks field equals the pinned hook contract address, yielding the PoolKey, its 32-byte keccak256 PoolId, and the Initialize block, with pool_init_outside_window + search bounds emitted (and the second pool excluded) when no hook-matched Initialize is found inside the candidate window. (2) Deliverable 1 (lines 74-84) is rewritten to match: hook-matched Initialize scan, resolved PoolKey fields, 32-byte PoolId = keccak256(abi.encode(PoolKey)), offline re-derivation check, and pool_init_outside_window exclusion. (3) The Acceptance clause (lines 108-111) is updated to require the 32-byte PoolId to re-derive from the resolved PoolKey only after the pinned hook contract address matches an Initialize log (hooks == 0xEd50bDeeA8aDC232f159486192a4157281D722ff), and a mismatch halts the run. The pool_init_outside_window clause (lines 117-119) is extended to cover the second pool with search bounds recorded. (4) The Must-not clause (lines 147-151) forbids guessing PoolKey/fee/tickSpacing/hooks from the pinned hook contract address, from a symbol, or from another pool's bytecode, and forbids guessing the 32-byte PoolId from the pinned hook contract address or any other identifier without an Initialize log match where hooks equals the pinned address. (5) docs/spec/protocol/PROVIDER_FACTS.md line 124 is re-labeled identically: 'Owner-pinned by hook contract address', described as a lookup signal equal to the hooks field, explicitly not a V4 PoolId and not a keccak256 digest, with T038 resolving PoolKey, 32-byte PoolId and Initialize block via Initialize logs whose decoded hooks equals the pinned address, and T038 then verifying keccak256(abi.encode(PoolKey)) == PoolId where PoolId is the 32-byte chain-emitted digest. The same 20-byte value is therefore re-labeled consistently across the contract and the spec. (6) todo/config.yaml spec_revision is bumped from v1-2026-09-18 to v1-2026-09-18-t038-hook-pin, following the established label pattern and explicitly tracking this Spec-layer change. (7) The diffstat confirms only the four expected files changed: docs/spec/protocol/PROVIDER_FACTS.md, todo/config.yaml (spec_revision bump plus the controller-applied workflow_state and T038.status transition from PLANNING to AWAITING_PLAN_REVIEW, with all other config fields untouched), todo/evidence/P03/T038/attempt-001-planner.json (new planner evidence file with PLAN_READY outcome, schema-valid against todo/schemas/planner-result.schema.json), and todo/phases/P03-ingestion-and-storage/T038.md. No other task contract (T030-T037, T036, T040-T043) was touched; no Intent, workflow/controller code, agent definitions, AGENTS.md, CLAUDE.md, todo/README.md, pyproject.toml, dependency manifest, src/ or tests/ tree was touched. (8) Pre-existing clauses are preserved verbatim: Outcome, Dependencies (T036, T037), the Window rule section, Deliverables 2-6, the pre-existing Acceptance bullets (finalized-block pin, complete=true on T034, cross-endpoint sampling, runbook determinism, range-based acquisition, RPC/HTTP budget reporting, T023 hook classification, pytest/Ruff/mypy pass), the pre-existing Must-not bullets (latest ban, dataset immutability, window-cap rule, cross-endpoint independence, complete=true integrity, hook-modeling honesty, reference-pool immutability), and the References list (T022, T023, T030-T037, ADR-002, ADR-010, ADR-012, PROVIDER_FACTS.md, R7, R9, R10, R24, R25). Contract status header (OWNER_PLAN_EXTENSION_2026-09-18) is unchanged. (9) The Planner's recommended Developer next-step is correct: the existing RESOLVE_PINNED_POOL_ID_SIZE_DEFECT branch in src/robinhood_lp/qualification/second_pool.py is superseded by contract and must be reworked to a hook-address-scanning resolver that filters Initialize logs by hooks == 0xEd50bDeeA8aDC232f159486192a4157281D722ff, decodes the matching PoolKey, verifies keccak256(abi.encode(PoolKey)) equals the 32-byte chain-emitted PoolId, and emits pool_init_outside_window with search bounds when no match is found inside the candidate window; the corresponding tests in tests/test_two_pool_t038.py must be refreshed accordingly.

## Required changes

- None.

## Unknowns

- None.
