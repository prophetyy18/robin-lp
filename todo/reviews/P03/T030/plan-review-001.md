# T030 planning review

- Base commit: `503ce7483b4396f62662eeb71aeade9566107f74`
- Candidate commit: `9a4437865e06a048848b8682c3cdccdb9e50e3a2`
- Verdict: **PASS**

## Summary

The planning candidate merges the previously dual-layer T030 contract (FROZEN_FROM_BASELINE_6C31778 marker plus the standalone 2026-09-16 Owner amendment block) into a single unified contract body as required by Owner decision #5. The top frozen marker and the standalone amendment paragraph are both removed; the contract body now incorporates every binding instruction from /tmp/owner_decision_T030.txt into the Outcome, Deliverables, Acceptance, Must-not and References sections. Owner decision #1 is satisfied: the Implementation status section explicitly labels src/robinhood_lp/storage/schema.py and the related storage-schema tests as pre-existing unverified implementation under the WORKFLOW.md migration rule (preserve, review per task, claim after approval), and the Still open subsection enumerates every gap (dependency verification against T011 EventKey / BlockRef / TransactionRef / PoolIdentity and T021 pinned v4-core ABI artifact, full typed reconstruction for every V4 event, ProtocolFeeUpdated(bytes32,uint24) schema and fixtures, real schema-v1 migration fixture and migration test, strict separation of acquisition provenance from normalized event identity / content hash, and every acceptance test listed under Acceptance). Owner decision #2 is satisfied: the Outcome section explicitly assigns T030 the logical schema (record types, field names, types, widths, identity, semantics) and assigns T031 the Parquet column layout, partition strategy, and manifest/checkpoint/reorg journal physical layout under ADR-002; ADR-002 is not reopened and ADR-010's field-semantics refinement is honored (acquisition envelope/manifest holds observational endpoint alias, retrieval time, request interval, and HTTP batch metadata; normalized event identity uses T011 EventKey (chain_id, block_hash, tx_hash, log_index) and the normalized content hash excludes observational fields so the same on-chain event fetched from A and B produces the same content hash while both raw acquisition envelopes are retained and checksummed). All five V4 events (Initialize, ModifyLiquidity, Swap, Donate, ProtocolFeeUpdated) appear in the Deliverables schemas and event-fields enumerations; the T021 pinned-artifact extension and selector/topic drift test for ProtocolFeeUpdated(bytes32,uint24) is explicitly required. The Acceptance section preserves every required clause: raw JSON to normalized to canonical round trip preserves every source byte; compatibility and migration tests cover unknown fields plus an old schema/decode version replayed through the current decoder; fixtures assert exact sign and width semantics with deterministic ordering by (block_number, transaction_index, log_index); same on-chain event fetched from two endpoints at two retrieval times yields the same normalized content hash while retaining both raw envelopes; same-height fork logs remain distinct by block hash; request provenance never contains a credential-bearing URL, asserted by a dedicated test. The Must-not section forbids overwriting raw values with decoded or display values, storing integers as floats, making token metadata required, writing a credential-bearing URL into any layer, collapsing two observational acquisitions into one envelope, and mutating pre-existing unverified implementation outside the T030 contract or fast-forwarding it into APPROVED without an independent Reviewer verdict. References include R4, R9, ADR-002, ADR-010, ADR-011. The trailing Binding direction reference subsection is presented as an audit trail only and explicitly states it is not a second binding source. Path compliance is satisfied: the diffstat shows only todo/config.yaml (PLANNING to AWAITING_PLAN_REVIEW workflow-state transition for T030 plus the corresponding workflow_state field, with attempt/base_commit/candidate_commit/approved_commit/latest_review untouched), todo/evidence/P03/T030/attempt-001-planner.json (new planner evidence file), and todo/phases/P03-ingestion-and-storage/T030.md. No docs/intent/, docs/spec/, other task contracts, workflow/controller code, agent definitions, review record, src/, tests/, tools/, AGENTS.md, CLAUDE.md, or README.md was touched; task attempt counter, evidence pointers, commit SHAs, model selection, and approval data in todo/config.yaml were not changed by the contract merge. The Developer can execute attempt 002 directly against this merged contract.

## Required changes

- None.

## Unknowns

- None.
