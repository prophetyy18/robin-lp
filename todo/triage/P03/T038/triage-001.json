{
  "task_id": "T038",
  "issue_commit": "c11fbc730933768254b013ee4971f6374e796e9a",
  "classification": "OWNER_DECISION_REQUIRED",
  "summary": "The T038 contract pins the second pool's identity with the literal label 'PoolId' but supplies `0xEd50bDeeA8aDC232f159486192a4157281D722ff` (40 hex chars / 20 bytes / 160 bits, address-sized). `docs/spec/protocol/PROTOCOL_FACTS.md` (line 113) and `src/robinhood_lp/protocol/ids.py` (POOL_ID_BYTES=32, PoolId is bytes32) define a V4 PoolId as `keccak256(abi.encode(PoolKey))` = 32 bytes / 64 hex chars / 256 bits, and the same 20-byte value is also recorded in `docs/spec/protocol/PROVIDER_FACTS.md` line 124 with the same mislabel. The Developer correctly refused to guess the PoolKey, surfaced `RESOLVE_PINNED_POOL_ID_SIZE_DEFECT`, and asked T038 to halt (T038.md Acceptance: 'a mismatch halts the run rather than being reconciled by hand'; Must-not: 'guess the second pool's PoolKey from its PoolId'). The defect is therefore contractually correct: the contract and the Spec are unusable as written, but only the Owner can resolve what this 20-byte value actually represents and supply the matching 32-byte keccak256 digest, because T038.md Must-not forbids deriving the PoolKey from the PoolId and the Owner is the unique source of the second-pool identity. The defect spans both the T038 contract and `docs/spec/protocol/PROVIDER_FACTS.md` (a Spec-layer file), so a future Planner with SPEC-layer permission can amend both once the Owner direction is recorded; the Developer implementation itself is sound (910 non-oracle tests, mypy strict, ruff format/check pass) and need not be redone.",
  "evidence": [
    "/home/lpdev/lp/todo/phases/P03-ingestion-and-storage/T038.md line 29: 'second pool `PoolId` `0xEd50bDeeA8aDC232f159486192a4157281D722ff` (Owner-pinned, chain id `4663`)'; the contract also requires `keccak256(abi.encode(PoolKey)) == PoolId` (line 33), forbids guessing the PoolKey from the PoolId (line 127 Must-not), and requires halting on mismatch (line 92 Acceptance).",
    "/home/lpdev/lp/docs/spec/protocol/PROVIDER_FACTS.md line 124: 'The second pool of the window is Owner-pinned by `PoolId` `0xEd50bDeeA8aDC232f159486192a4157281D722ff` on chain id 4663 ... they are unresolved and are resolved on chain by T038, which then verifies `keccak256(abi.encode(PoolKey)) == PoolId`' — the same 20-byte identifier is also pinned in the Spec layer.",
    "/home/lpdev/lp/docs/spec/protocol/PROTOCOL_FACTS.md line 113-127 (PoolId section, Source: `src/types/PoolId.sol`): '`PoolId` is a 32-byte value (a `bytes32`).' — the protocol defines PoolId as 32 bytes, contradicting the 20-byte value pinned in T038.md and PROVIDER_FACTS.md.",
    "/home/lpdev/lp/src/robinhood_lp/protocol/ids.py lines 36-37, 247-280: ADDRESS_BYTES=20, POOL_ID_BYTES=32; `PoolId` rejects any input whose body length is not 64 hex chars; `PoolKey.to_pool_id()` returns `keccak256(abi.encode(PoolKey))`; `PoolId.from_hex` raises ValueError on '0x' + body lengths other than 66.",
    "/home/lpdev/lp-worktrees/dev-t038-attempt-001/.workflow/todo/evidence/P03/T038/attempt-001-developer.json (developer TRIAGE_REQUIRED handoff): 910/910 non-oracle tests pass, ruff format/check and mypy strict pass; resolver surfaces `RESOLVE_PINNED_POOL_ID_SIZE_DEFECT` with explicit error_detail naming the size mismatch; Developer proposes CONTRACT_MISMATCH and asks for an Owner amendment to either replace the 20-byte value with the canonical 32-byte digest or supply the 32-byte PoolId alongside.",
    "/home/lpdev/lp-worktrees/dev-t038-attempt-001/src/robinhood_lp/qualification/two_pool_window.py line 45: SECOND_POOL_POOL_ID_HEX = \"0xEd50bDeeA8aDC232f159486192a4157281D722ff\" — the 40-char body is the sole source of the size mismatch in the implementation; the implementation does not silently truncate, pad, or guess.",
    "/home/lpdev/lp-worktrees/dev-t038-attempt-001/tests/test_two_pool_t038.py: `test_owner_pinned_pool_id_is_20_bytes_contract_defect` and `test_two_pool_report_incomplete_with_owner_pinned_20_byte_pool_id` document the contract defect end-to-end and pin the resolver to `RESOLVE_PINNED_POOL_ID_SIZE_DEFECT`, so any future Planner amendment does not need to repeat the test design.",
    "/home/lpdev/lp/todo/WORKFLOW.md (Owner-directed amendments before implementation, lines 80-107): the Planner with CONTRACT or SPEC layer permission can amend the T038 contract and `docs/spec/protocol/PROVIDER_FACTS.md` once a recorded `--owner-direction` is supplied; without that direction, the Planner cannot guess the correct value (T038 Must-not).",
    "/home/lpdev/lp/todo/triage/P03/T034/triage-001.json (T034 precedent): CONTRACT_MISMATCH was used when an explicit, recorded Owner direction already existed to consolidate; the present case has no clarifying Owner record — only the ambiguous 20-byte pin — so it is an Owner-decision gate, not a planning task that can proceed independently."
  ],
  "recommended_action": "Stop and ask the Owner to resolve what `0xEd50bDeeA8aDC232f159486192a4157281D722ff` (chain id 4663) actually represents and to supply the matching canonical 32-byte V4 PoolId. The Owner must choose one of the following two paths and provide the explicit direction required by `todo/WORKFLOW.md` 'Owner-directed amendments before implementation': (a) the 20-byte value is the wrong identifier — replace the pin in T038.md line 29 and PROVIDER_FACTS.md line 124 with the correct 32-byte keccak256 digest of the resolved PoolKey, or (b) the 20-byte value is intentional but refers to a different concept (e.g., a hook contract address, a deployer, or another on-chain reference); in that case the Owner must supply the actual 32-byte PoolId and clarify the role of the 20-byte value, and the contract/Spec must be reworded to label each identifier correctly. After the Owner direction is recorded, the Manager routes through `prepare-amendment --task T038 --layer SPEC --owner-direction \"<exact Owner direction>\"` so a single Planner (SPEC layer is required because both T038.md and `docs/spec/protocol/PROVIDER_FACTS.md` must change together) rewrites the contract label/identity, updates the PROVIDER_FACTS entry, preserves the existing implementation's `RESOLVE_PINNED_POOL_ID_SIZE_DEFECT` branch as a defensive guard, and an independent Plan Reviewer verifies the merge. The Developer implementation in `dev-t038-attempt-001` does not need to be redone: 910 tests pass, ruff format/check and mypy strict pass, and the resolver already refuses to silently truncate/pad/guess; a fresh Developer after the amendment simply flips the defect branch off and re-runs the existing tests once the pin is corrected.",
  "owner_question": "The T038 contract and `docs/spec/protocol/PROVIDER_FACTS.md` both pin `0xEd50bDeeA8aDC232f159486192a4157281D722ff` (chain id 4663) with the literal label `PoolId`, but a V4 PoolId is `keccak256(abi.encode(PoolKey))` = 32 bytes / 64 hex chars (per `docs/spec/protocol/PROTOCOL_FACTS.md` line 113 and `src/robinhood_lp/protocol/ids.py` `POOL_ID_BYTES=32`), and this value is only 20 bytes / 40 hex chars (address-sized). T038.md Must-not forbids deriving the PoolKey from the PoolId and T038.md Acceptance requires the run to halt on a keccak256 mismatch, so the Developer cannot resolve this without Owner input. Please choose one of the following and supply the exact direction the Manager will pass to `prepare-amendment --owner-direction`: (1) the 20-byte value is incorrect — supply the canonical 32-byte keccak256 digest of the second pool's PoolKey so both T038.md line 29 and PROVIDER_FACTS.md line 124 can be amended to that digest; or (2) the 20-byte value is intentional but refers to a different concept (e.g., a hook contract address or deployer) — supply the actual 32-byte PoolId and state the role of the 20-byte value so both T038.md and PROVIDER_FACTS.md can be reworded to label each identifier correctly. Without this direction the Planner cannot amend the contract or the Spec without guessing, and the Developer run cannot proceed."
}
